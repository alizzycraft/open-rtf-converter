# Open RTF converter progress

- Word-compatible legacy arrowhead suppression (2026-08-03): the registered Word 16.0 `shape-arrowheads-passive.rtf` baseline proves that Office `lineStartArrowhead`, `lineEndArrowhead`, and arrow-size properties are ignored inside legacy `\\do` drawings. Word-compatible parsing now consumes all six properties without changing normalized geometry, while StrictSpec and modern shapes retain bounded passive arrows. Word and corrected converter PDFs both emit exactly one butt-capped solid line at `(90,96)-(162,132)`, with converter's 0.15-point bounded hairline versus Word's 0.14-point rounding; neither output contains arrowhead paths. Text flow matches with the existing roughly 0.96-point bundled Arial-compatible vertical residual. Both PDFs were rendered at 2x and visually inspected; arrow geometry/security tests now explicitly run in StrictSpec. The complete 2,253-test all-target suite passes (1,164 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,009 security, and 2 WASM-policy), with formatting and `git diff --check` clean.

- Word-compatible floating legacy drawings and evidenced Office-property boundary (2026-08-03): three registered Word baselines (`shape-line-cap`, `shape-line-dashing`, and `shape-arrowheads`) prove paragraph-anchored `\\do` drawings are floating and do not consume body flow. Layout now anchors consecutive shapes to the preceding paragraph origin and leaves the following paragraph at normal cadence; all three main lines move from `(90,109.80)-(162,145.80)` to Word's exact `(90,96)-(162,132)`. The corrected following line starts at y=85.28 versus Word's y=86.24, the existing bounded Arial glyph-box residual. The cap/dash baselines independently prove those Office `\\sp` properties are ignored inside legacy `\\do` even though direct `\\dp...` controls and modern `\\shp` properties remain active. Word-compatible parsing ignores the evidenced legacy cap/dash/arrow properties and uses a bounded 3-twip hairline; StrictSpec retains property interpretation. Corrected outputs emit plain butt-capped solid 0.15-point lines versus Word's 0.14-point rounding with identical endpoints and flow. All six PDFs were rendered at 2x and representative pairs were visually inspected.
- Floating legacy drawing verification (2026-08-03): the complete all-target suite passes: 1,162 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 executable/reference-policy, 1,009 security, and 2 WASM-policy tests (2,251 total). Formatting and `git diff --check` are clean.

- Focused WMF-text Word audit (2026-08-03): `wmf-text-passive.rtf` confirms the same unresolved inline-picture architecture gap as the vector fixture. Word keeps the 72-by-36-point metafile between surrounding paragraph runs: body spans start at `(36.02,61.16)` and continue at `(201.43,61.16)`, while the malformed EXTTEXTOUT record exposes only a clipped 6.52-point bold `H` at `(147.58,44.55)`. The converter instead emits a separate block, paints `Hi` in 4-point Helvetica at `(54.00,58.50)`, and starts the following paragraph at y=85.28. Both PDFs were rendered at 2x and visually inspected. This needs the already-identified bounded inline-image model plus metafile text clipping/font selection; a local baseline adjustment would preserve the wrong flow architecture.

- Word-compatible emboss/imprint state and paint calibration (2026-08-03): `text-effects-passive.rtf` and its decoded Word PDF operators prove Word tracks `\\embo` and `\\impr` independently but keeps emboss visually dominant while both are active; `\\impr0` therefore leaves emboss unchanged for `level` and the entire following paragraph. Direct and list-level parser state now preserve that precedence, group scoping, and `\\plain` reset behavior. At 12pt Word paints relief at the nominal origin in 60%-gray, at (-1.92,+1.92) points in dark, and at (-0.96,+0.96) in the source color. The passive PDF writer now reproduces that three-layer geometry using an evidenced 8%-of-font-size unit offset with bounded extremes. Word/current PDFs were rendered at 2x and visually inspected; relief strength, direction, persistence, outline placement, and transformed casing align.
- Emboss/imprint verification (2026-08-03): the complete all-target suite passes: 1,160 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 executable/reference-policy, 1,009 security, and 2 WASM-policy tests (2,249 total). Formatting and `git diff --check` are clean.

- Focused table keep-pagination Word audits (2026-08-03): `table-row-keep-together-passive.rtf` and `table-row-keep-follow-passive.rtf` both match Word's two-page grouping. The 72-point kept row stays intact on page two; the keep-follow/follower pair moves together; subsequent normal rows and post-table flow retain the same 36-point cadence. Horizontal origins differ by 0.02 points and vertical origins by no more than about 1.0 point from bundled Arial-compatible metric boxes. All eight pages were rendered at 2x and the representative kept-row page was visually inspected; both stale pagination gaps are closed without code changes.

- Word-compatible non-facing section parity (2026-08-03): registered odd/even section-break PDFs both show Word inserting a blank physical page two and starting the next section on right-hand page three when `\\facingp` is absent. Layout and page-variant estimation now normalize an even-page break to odd/right-hand parity only for non-facing documents; authored facing-page documents retain true even starts. `even-section-break-passive.rtf` now matches Word's three pages and page-three origin exactly at 2x, and focused layout coverage proves both branches. The stale odd/even parity gaps are closed.
- Non-facing section parity verification (2026-08-03): all 1,159 library tests, 40 conversion tests, 4 executable/reference-policy tests, and the focused odd-section security regression pass. Formatting and `git diff --check` are clean.

- Word-compatible legacy no-break section recovery (2026-08-03): two registered Word baselines (`section-break-kinds-passive.rtf` and `section-number-passive.rtf`) prove that Word recovers `\\sbknone` as a page section break in these legacy streams, yielding three one-paragraph pages rather than continuous first/second sections. Word-compatible parsing now matches that visible behavior while StrictSpec retains the formal continuous interpretation. Both corrected fixtures produce the same three 306-by-396-point pages and section numbering as Word; page-two PDFs were rendered at 2x and visually inspected. Focused parser tests pass.
- Legacy no-break section verification (2026-08-03): all 1,159 library tests, all 40 conversion tests, all 4 executable/reference-policy tests, and the focused section-number security regression pass. Formatting and `git diff --check` are clean.

- Focused widow-control Word audit (2026-08-03): Word and the converter have identical four-page grouping on `widow-control-passive.rtf`: page one ends after `Filler two`, page two after `Filler six`, page three after `Restored tail`, and `Alias plain tail` starts page four. Horizontal origins match exactly and bundled Arial-compatible metric-box differences do not affect pagination. All eight page renders were generated at 2x; the stale pagination gap is closed without a code change.

- Word table-row-height audit (2026-08-03): the registered Word baseline confirms 36-point minimum-height expansion, 18-point exact-height clipping, first-cell-only shading/borders, and immediate post-table flow. Parser-created cell definitions now start with hidden sides so explicit borders do not leak onto an unauthored neighboring cell. The converter visually matches the baseline with remaining vertical differences below about 0.3 points; focused parser regressions pass.
- Table-row-height verification (2026-08-03): full all-target verification passes: 1,158 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,009 security, and 2 WASM-policy tests (2,247 total). Formatting and `git diff --check` pass.

- Table-padding Word audit (2026-08-03): row-default padding matches Word for the first cell (x=54 within 0.02 points), but Word places the second cell at x=168.05 on the same baseline even though its `\clpad*` controls occur after first-cell text has begun. The converter applies those late controls literally (x=159 and 3 points lower). This is now recorded as a probe-gated malformed-row recovery/precedence gap; changing it from the single ambiguous fixture would risk valid cell-definition behavior.

- Word font line-gap and populated-row height refinement (2026-08-03): `intbl-zero-body-flow-passive.rtf` exposed that safe supplied-font metrics used only ascender minus descender and then hit a hard 14-point floor, while Word uses the font's bounded `hhea` line gap for a 13.80-point Arial pitch. `FontGlyphMetrics` now retains nonnegative line-gap units, first visible runs may replace the empty-line fallback, and populated table rows use measured content height instead of a 14-point minimum. Body-to-table and table-to-body pitch now matches Word at 13.80 points with no cumulative drift; substitute glyph boxes remain a constant 0.95 points high and x differs by 0.02 points. Fit-text cells retain pre-scaled word runs when already bounded. Both PDFs were rendered at 2x and visually inspected; 1,157 library tests and focused security regressions pass.
- Font line-gap verification (2026-08-03): full all-target verification passes: 1,157 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,009 security, and 2 WASM-policy tests (2,246 total). Formatting and `git diff --check` pass.

- Header/footer-distance Word audit (2026-08-03): the registered Word 16.0 baseline confirms the authored header/footer distances, 72-point horizontal origin, and body margin geometry. Converter text boxes are 0.66 points above Word for header/body and 0.95 points above for the footer, consistent with the existing bounded bundled Times-compatible metric-box approximation. Both PDFs were rendered at 2x and visually inspected; the stale focused-audit gap is closed without a code change.

- No-wrap compatibility refinement (2026-08-03): Word-compatible parsing now ignores the nonstandard `\nowrap` alias while strict mode retains it; standard `\nowwrap` and `\clNoWrap` suppress word-boundary wrapping but retain bounded emergency character wrapping, including carrying a break-adjacent space onto the next line. Focused parser/layout tests and a rendered Word comparison pass for line-breaking behavior. Remaining baseline gap: Word starts the final fixed-height table row in the small remaining page area and clips it at the page boundary, while the converter moves that row to page 2.
- No-wrap verification (2026-08-03): full all-target verification reached the reference harness after 1,156 library, 32 CLI, 40 conversion, 1,009 security, and 2 fuzz-regression tests passed. The reference manifest now records the intentional current two-page converter output separately from Word's one-page clipped-row baseline; all 4 reference-policy tests pass after that explicit contract update. Formatting and `git diff --check` pass.

- Word-compatible exact-font priority and distributed final-line alignment (2026-08-03): the registered `alignment-direction-passive.rtf` Word 16.0 baseline exposed that the bundled wildcard sans asset was selected before the exact Times New Roman alias, despite parser normalization already supplying Word's implicit Times default. Font metric selection and PDF embedding now rank exact/canonical family aliases ahead of wildcard fallbacks while retaining wildcard coverage for genuinely unknown families. The restored Tinos metrics reproduce all Word line breaks. `\\qd` now remains distinct bounded distributed metadata: non-final lines use passive word spacing and the final line uses bounded PDF character spacing, while `\\qk`/`\\qt` retain normal final lines. Right/left paragraph placement and passive Hebrew/LTR ordering match Word; the distributed final endpoint is 3.13 points short from Tinos metrics, right alignment differs by 0.17 points, and vertical origins by at most 0.92 points. Word and corrected PDFs were rendered at 2x and visually inspected. Security leak assertions now inspect decoded PDF object/content streams while excluding only referenced trusted embedded-font programs, preventing compressed font bytes from masquerading as short RTF controls without weakening active-PDF or visible-payload checks. Verification passes: 1,155 library, 32 CLI, 40 conversion, 1,009 security, 4 reference-policy, 2 fuzz-regression, and 2 WASM-policy tests (2,244 total); formatting and `git diff --check` pass.
- Focused landscape-zero/section-size Word audit (2026-08-03): the registered `landscape-zero-geometry-passive.rtf` Word 16.0 reference confirms that explicit zero-valued landscape controls retain portrait geometry and that the later section's bounded page-size controls preserve the same media box. Word and converter both emit three exact 612-by-792-point pages, each paragraph starts at the same 72-point margins, and origins differ by 0.02 points horizontally and 0.85 points vertically from bundled Times-compatible metrics. Representative PDFs were rendered; the stale media-box audit gap is closed without a code change.
- Word-compatible parallel/top gutter geometry (2026-08-03): the registered `page-gutter-passive.rtf` Word 16.0 baseline exposed two conflated controls. `\\gutterprl` is a document-wide parallel gutter, which adds the bounded gutter to the top margin for horizontal text; it is not an alias for the bidirectional right-side `\\rtlgutter`. Word also ignores the fixture's nonstandard `\\margmirsxn` alias, while standard `\\margmirror` remains supported and StrictSpec retains bounded alias interpretation. The safe page model now represents top and right gutters independently, parallel positioning propagates across existing/future sections, and horizontal gutter width is removed when the gutter is at the top. All three pages now match Word at x=36 within 0.02 points and y=72 within 0.80 points. Word and corrected page-two/page-three PDFs were rendered at 2x and visually inspected. Verification passes: 1,153 library, 32 CLI, 40 conversion, 1,009 security, 4 reference-policy, 2 fuzz-regression, and 2 WASM-policy tests (2,242 total); formatting and `git diff --check` pass.
- Retroactive section-level page vertical alignment (2026-08-03): the registered `page-vertical-alignment-passive.rtf` Word 16.0 baseline exposed that section properties are retroactive within their section. Word's final `\\vertalt` resets the earlier `\\vertalc` for both paragraphs, while the converter formerly snapshotted center alignment after the first paragraph and displaced both lines to y=175.64. Section settings now update the current section's existing safe metadata (or are inserted immediately after its real section boundary) instead of appending a late layout transition. The corrected lines return to top flow within 0.80 and 0.60 points vertically and 0.02 points horizontally; later-section regression coverage proves updates do not cross section boundaries. Word and corrected PDFs were rendered at 2x and visually inspected. Verification passes: 1,152 library, 32 CLI, 40 conversion, 1,009 security, 4 reference-policy, 2 fuzz-regression, and 2 WASM-policy tests (2,241 total); formatting and `git diff --check` pass.
- Focused page-break-before Word audit (2026-08-03): the registered `page-break-before-passive.rtf` Word 16.0 reference confirms exact two-page grouping. `First page text` is alone on page one, `\\pagebb` moves `Second page text` to the top of page two, and `\\pagebb0` leaves `Still second page` directly below it. Converter and Word origins differ by 0.02 points horizontally and at most 0.80 points vertically from bundled Arial-compatible metrics. Both page-two PDFs were rendered at 2x and visually inspected; the stale focused-audit gap is closed without a code change.
- Word-compatible automatic/contextual paragraph spacing (2026-08-03): the registered `paragraph-spacing-passive.rtf` Word 16.0 baseline proves that parameterless `\\sbauto`/`\\saauto` do not synthesize the converter's former 12-point passive gaps and that contextual spacing suppresses an adjacent same-style boundary when either side opts in. Word-compatible parsing now leaves automatic spacing inert while StrictSpec retains the bounded 12-point interpretation; contextual boundary collapse uses either adjacent flag in body and table flow. The corrected five baselines follow Word's normal cadence with diminishing bundled-Arial vertical residuals of 0.80, 0.60, 0.40, 0.20, and 0.00 points. Word and corrected PDFs were rendered at 2x and visually inspected. Verification passes: 1,150 library, 32 CLI, 40 conversion, 1,009 security, 4 reference-policy, 2 fuzz-regression, and 2 WASM-policy tests (2,239 total); formatting and `git diff --check` pass.
- Word-compatible multiple-line baseline placement (2026-08-03): the registered `line-spacing-passive.rtf` Word 16.0 baseline exposed that positive `\\slN\\slmult1` expands baseline advance without pushing the first line's glyphs to the bottom of the enlarged box. Layout lines now retain bounded natural glyph height separately from their spacing/pagination height; multiple spacing uses the natural first baseline while exact/at-least spacing preserves box-relative behavior. The former 7.86-point first-line displacement is reduced to a 2.64-point bundled-Arial leading residual, the second double-spaced line is within 0.20 points, exact-line two within 0.05 points, and every following line within 0.39 points. Word and corrected PDFs were rendered at 2x and visually inspected; extreme spacing still recovers to automatic spacing with a diagnostic. Verification passes: 1,148 library, 32 CLI, 40 conversion, 1,009 security, 4 reference-policy, 2 fuzz-regression, and 2 WASM-policy tests (2,237 total); formatting and `git diff --check` pass.
- Word-compatible resultless FILESIZE and malformed ADVANCE recovery (2026-08-03): the registered `environment-layout-clock-fields-passive.rtf` Word 16.0 PDF disproves the prior metadata synthesis behavior. Word leaves resultless FILESIZE blank even with `\\nofbytes4096`, and the fixture's malformed RTF-level `\\r`/`\\d` controls are consumed before field evaluation, leaving literal `240 120` arguments visible without hidden cursor movement. Word-compatible mode now suppresses FILESIZE while StrictSpec retains bounded metadata rendering; zero-offset ADVANCE recovers up to four bounded integer arguments as passive literal text only when no real field switches survive. Strip mode now matches all non-clock/host lines, with the ADVANCE line within 0.07 points horizontally. Word, Strip, and Placeholder PDFs were rendered and visually inspected; dynamic DATE/TIME and environment placeholders remain intentional no-host-state security differences. Verification passes: 1,147 library, 32 CLI, 40 conversion, 1,009 security, 4 reference-policy, 2 fuzz-regression, and 2 WASM-policy tests (2,236 total); formatting and `git diff --check` pass.
- Focused non-visible field whitespace/security Word audit (2026-08-03): the registered `nonvisible-storage-fields-passive.rtf` Word 16.0 PDF confirms that stripping ADDIN, PRIVATE, PRINT, external RD, XE, TC, and TA leaves both authored surrounding spaces exactly intact. Converter and Word preserve the same nine visible lines, no hidden payload/link appears, horizontal origins differ by 0.02 points, and bundled Arial-compatible 14-point pitch versus Word's 13.8-point pitch accumulates to only 0.77 points by the final line. Both PDFs were rendered and visually inspected; all active/external/non-visible field warnings remain explicit.
- Focused stored-hyperlink/external-field Word security audit (2026-08-03): the Word 16.0 `field-hyperlink-passive.rtf` reference confirms that HYPERLINK's stored result is concatenated with surrounding text without synthetic spacing and that resultless INCLUDEPICTURE is silently omitted. Explicit Strip mode matches Word's two visible lines within 0.07 points horizontally and 0.8 points vertically while emitting no link annotation/URI action and never fetching the external image. Default Placeholder intentionally adds one bounded `[Field removed: no passive result]` line, preserving the configured security disclosure rather than Word's silence. Word, Strip, and Placeholder PDFs were rendered and visually inspected; the stale spacing gap is now replaced with measured compatibility and intentional-policy evidence.
- Measured unresolved inline-WMF model gap (2026-08-03): the focused `wmf-vector-passive.rtf` audit shows that Word 16.0 interleaves the 108-by-36-point metafile with text before and after the `\\pict` group, whereas the normalized model currently flushes both paragraphs and emits a separate image block. Word's rectangle is `(82.77,53.78)-(157.77,74.59)` and ellipse `(125.20,57.70)-(163.78,78.02)` with 1.88-point stock-pen strokes; current geometry is `(46.80,53.60)-(133.20,78.80)` and `(90.00,57.20)-(138.60,82.40)` with 0.45-point strokes. Both PDFs were rendered and visually inspected. The manifest now records the required bounded inline-image model and stock-pen calibration explicitly; a local coordinate tweak was rejected because it would retain the wrong flow architecture.
- Focused PNG alpha/indexed-transparency Word audits (2026-08-03): compared `png-alpha.rtf` and `png-trns.rtf` against their registered Word 16.0 PDFs. Both converter paths emit the exact Word 2-by-1 soft mask `[0, 255]`, the same opaque blue sample, a 72-by-36-point frame within 0.2 points vertically, and following text within 0.48 points. Word rewrites the fully transparent sample's hidden base color to black while the converter retains source red; because mask alpha is exactly zero, that byte is mathematically incapable of affecting compositing on white, colored, patterned, or transparent page content. All four PDFs were rendered and visually inspected; the stale non-white-background gaps are now replaced with measured evidence.
- Word-compatible parameterless DIB recovery (2026-08-03): the registered Word 16.0 `dib-grayscale-passive.rtf` reference proves that malformed parameterless `\\dibitmap` is not recognized as a paintable picture type. Word suppresses the payload and keeps surrounding text contiguous on one line; the converter formerly decoded a 36-point grayscale image block and split the text across lines. Word-compatible parsing now distinguishes missing from valid numeric parameters, strips payload/properties, retains a zero-width passive anchor, and rejoins the paragraph that picture-group entry had provisionally flushed. The corrected PDF has no image XObject and matches Word's one-line origin within 0.02 points horizontally and 0.8 points vertically. Parameterized `\\dibitmap0` retains bounded DIB decoding and grayscale/bilevel/tone tests. Both PDFs were rendered and visually inspected. Verification passes: 1,145 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,009 security, and 2 WASM-policy tests; formatting and `git diff --check` pass.
- Word-compatible legacy Windows-bitmap suppression (2026-08-03): the registered Word 16.0 `windows-bitmap.rtf` reference proves that modern Word does not paint this legacy `\\wbitmap0` DDB payload; it retains only the containing empty paragraph. Word-compatible passive parsing now replaces the payload with a normalized zero-width formatting anchor, preserving inline/empty-paragraph flow without carrying pixels or metadata into layout/PDF. The following paragraph moved from 21.72 points below Word to within 0.28 points, and both PDFs contain zero image XObjects. StrictSpec retains the separately tested bounded 2-by-2 bottom-up BGR-to-top-down RGB decoder. Word/current PDFs were rendered and visually inspected. Verification passes: 1,145 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,008 security, and 2 WASM-policy tests; formatting and `git diff --check` pass.
- Focused unknown-destination/control Word audit (2026-08-03): the registered `weird.rtf` Word 16.0 PDF confirms the bounded parser split between destination and in-flow recovery. The ignorable unknown destination and all of its text disappear; the unsupported `\\madeup123` control is consumed while its following text survives; escaped braces and `\\'hh` text decode visibly. Converter and Word output have identical two-line content, with origins within 0.02 points horizontally and 0.68 points vertically under bundled Times-compatible metrics. Both PDFs were rendered and visually inspected, and the converter emits the expected unsupported-control warning without leaking control syntax.
- Focused safe object-result Word/security audit (2026-08-03): compared `object-result.rtf` against its registered Word 16.0 PDF under both default Placeholder and explicit Strip active-content policies. All three outputs render only the same ordinary `Before object result.`, `visible fallback`, and `After object result.` paragraphs; the OLE payload is removed before safe-model normalization, and no removal placeholder is inserted when a safe static `\\result` exists. Text origins agree within 0.02 points horizontally and 0.68 points vertically, with no drawings or active PDF constructs. Word and converter PDFs were rendered and visually inspected.
- Word-compatible authored table-cell-gap flow (2026-08-03): the registered `table-ish.rtf` and `table-cell-text-direction-passive.rtf` Word 16.0 references proved that the previously calibrated 2.7-point post-table gap is not universal: it is half of the final row's bounded authored `\\trgaph108`, while omitted `\\trgaph` contributes zero. Layout now derives following flow from the final normalized row instead of adding a synthetic constant. The basic borderless table's following paragraph moved 2.7 points upward to within 0.28 points of Word, while the explicit-gap vertical table retained its 0.03-point match. Both pairs of PDFs were rendered and visually inspected. Verification passes: 1,145 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests; formatting and `git diff --check` pass.
- Focused basic-document Word audit (2026-08-03): replaced the stale missing-baseline note for `simple.rtf` after comparing the registered Word 16.0 PDF with a fresh converter PDF. Both outputs have two US Letter pages and matching visible text, 72-point margins, inline bold/italic/underline/red formatting, Unicode em dash fallback, centering, and explicit page break. Visible horizontal glyph positions agree within about 0.08 points; Word's extracted spans retain a non-painted trailing paragraph space, and bundled Arial-compatible glyph boxes retain the already-known roughly 0.8-point vertical offset. All four pages were rendered and visually inspected.
- Word-compatible paragraph-border flow, bounds, and dash cadence (2026-08-03): the Word 16.0 `paragraph-borders-passive.rtf` baseline exposed that paragraph borders sit outside the text line and consume vertical flow; the converter formerly drew them through the line box without reserving space, cumulatively shifting later text by up to six points. Layout now reserves bounded top/bottom/between stroke widths, extends horizontal strokes by Word's 1.5-point side gap, positions side strokes by gap plus half-width, distinguishes bottom-box from between-border centers, and includes top/bottom border reserves in keep preflight height. Passive dashed lines now use Word's 3:1 on/off width cadence instead of 3:2.25. Corrected box/bar/between coordinates differ by at most 0.14 points and text baselines by at most 0.64 points. Word and staged/final current PDFs were rendered and visually inspected. Verification passes: 1,144 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests; formatting and `git diff --check` pass.
- Word-compatible direct-overline recovery and underline weight (2026-08-03): the Word 16.0 `text-decoration-passive.rtf` baseline confirms word-only underline gaps, explicit red underline color, automatic black fallback, and ordinary underline thickness of about 0.84 points, but shows that Word entirely ignores direct `\\ol`, `\\ol0`, and `\\olnone`. Word-compatible parsing now consumes those controls without changing character style (including list-level definitions), while StrictSpec retains safe passive overline strokes and Office Math bars/radicals remain unaffected. Single/word underline strokes are calibrated from 0.5 to 0.8 points; endpoints differ from Word by at most 0.14 points. Word, corrected Word-compatible, and StrictSpec PDFs were rendered and visually inspected. Verification passes: 1,144 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests; formatting and `git diff --check` pass.
- Word-compatible parameterized bar tabs and authored table-border color (2026-08-03): focused Word 16.0 audits exposed two concrete defaults. Standard `\\tbN` directly defines a bar-tab position, while the fixture's malformed parameterless `\\tb` is ignored by Word; parsing now preserves bounded valid bar tabs but no longer turns malformed `\\tb` into persistent bar alignment or a spurious vertical stroke. The fixture's right/decimal/center/default/later-stop origins are within 0.05 points of Word, with only bundled-font dot-leader spacing remaining. Separately, authored table borders without `\\brdrcfN` now default to Word black instead of the converter's former 65% gray; deliberately synthesized nested-table grids retain their light fallback. The vertical-alignment fixture's x origins are within 0.07 points and following text within 0.03 points of Word, with a recorded 1.56-1.96-point cell-baseline/row-edge residual. Four Word/current PDFs were rendered and visually inspected. Verification passes: 1,143 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests; formatting and `git diff --check` pass.
- Word-compatible grouped character fit-text (2026-08-03): the Word 16.0 `character-typography-passive.rtf` baseline exposed that `\\fittextN` defines one target width across contiguous authored runs, while the converter previously stretched every wrapping segment/word independently to that full width. Layout now measures each bounded contiguous fit-text run group once, preserves relative authored `\\charscalex` differences, applies one shared horizontal scale, and clears fit metadata before wrapping. Under active-content `Strip`, the fixture's fitted region now remains on Word's first line and ends within 0.17 points of Word (172.75 versus 172.92 points); `Placeholder` intentionally adds a separate resultless-object security notice and is recorded as a visible security divergence. Word, Strip, and Placeholder PDFs were rendered and visually inspected. Verification passes: 1,141 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests; formatting and `git diff --check` pass.
- Word-compatible passive line-number typography (2026-08-03): the Word 16.0 `line-numbering-passive.rtf` baseline proves that margin numbers use 12-point black text aligned to the body baseline, not the converter's former 7-point gray approximation. Passive line numbers now use the evidenced size/color while retaining bounded numbering and the existing authored-distance right alignment. Their right edge exactly matches Word's 18-point boundary; the bundled Arial-compatible fallback starts 0.31 points farther left than Word's Aptos glyph, and body/number baselines share the same residual 0.8-point bundled-font vertical offset. Both PDFs were rendered and visually inspected. Targeted layout and security restart/suppression tests pass; multi-page Word restart behavior remains explicitly audit-gated.
- Word-compatible PNG alpha flattening and grayscale/bilevel conversion (2026-08-03): the Word 16.0 `picture-adjustments-passive.rtf` baseline confirms that transparent PNG samples are composited against black before picture color-mode processing and that grayscale uses Rec.709 luminance (opaque blue becomes 18, not the previous 29). Safe PNG decoding now inflates and unfilters bounded alpha masks, premultiplies RGB/gray samples against black, removes alpha, and then applies grayscale or bilevel conversion. The fixture's grayscale and bilevel image samples now match Word exactly; brightness/contrast tone mapping remains explicitly probe-gated because the Office specification defines the properties but not the pixel algorithm. Both PDFs were rendered and visually inspected. Verification passes: 1,139 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests; formatting and `git diff --check` pass.

- Completed Word 365 baseline registration (2026-08-03): copied all 171 successful Word 16.0 exports into the durable reference set and promoted their manifest entries to `available`. The five documents Word refused to export remain explicitly pending with their recorded Word-side failures; `docs/sample.rtf` is now included in the next small probe batch because the fixture-only exporter did not scan it. The Word contract now separates Word-specific page/text expectations from converter security-policy expectations, aggregates all failures in one run, and records the three confirmed section page-count divergences without weakening the converter's existing two-page gates. All 171 available PDFs pass parse/page/reference policy enforcement; 160 newly registered entries retain explicit focused-audit gaps instead of stale “once available” language.
- Word-compatible picture crop, scaling, and inline flow (2026-08-03): the Word 16.0 `picture-scaling-passive.rtf` baseline confirms that positive crop amounts reduce authored goal dimensions before `\\picscalex`/`\\picscaley` are applied, while a crop that consumes the tiny source's natural bitmap extent suppresses painting without removing its 30-by-60-point inline flow frame. Layout and PDF clipping now model those two coordinate spaces separately, fully cropped sources remain inert undrawn resources, and ordinary inline images no longer add a synthetic six-point trailing gap. The target fixture's following text is within about 0.7 points of Word; the independent three-image adjustment fixture now matches every following-line y position within 0.2 points. Both scaling PDFs and the adjustment geometry were rendered and visually inspected. Verification passes: 1,138 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Word-compatible vertical table text and following flow (2026-08-03): the Word 16.0 `table-cell-text-direction-passive.rtf` baseline exposed three coupled layout defects: separately emitted runs advanced across the cell instead of along the rotated baseline, wrapped vertical lines advanced down the row instead of across it, and every ordinary table inserted a synthetic six-point trailing gap. Rotated run origins now transform both advance and baseline shift, authored row height bounds vertical wrapping, clockwise/counter-clockwise columns progress in the correct direction, and the table-flow gap is calibrated to 2.7 points from five Word table baselines. The target fixture now matches Word's 72-point row, border/cell geometry, and following paragraph position (within 0.03 points); bundled Arial-compatible label widths remain about 3.3 points shorter. The Word PDF is registered, and both outputs were rendered and visually inspected. Verification passes: 1,136 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Word-compatible nonstandard first-line indent recovery (2026-08-03): Microsoft RTF 1.9.1 defines `\\linN` and `\\rinN` directional indents but not `\\finN`; Word 16.0 likewise ignores the fixture's nonstandard `\\fin-240` spelling. Word-compatible parsing now consumes that spelling without changing first-line indentation or leaking control text, while retaining bounded left/right geometry. `paragraph-indent-aliases-passive.rtf` now matches Word's 72-point x position within 0.02 points and differs vertically by at most about 0.7 points due to bundled Times-compatible metrics. The Word PDF is registered, and both outputs were rendered and visually inspected. Verification passes with the complete split suite.
- Complete keep-with-next chain pagination (2026-08-03): the Word 16.0 `paragraph-keep-pagination-passive.rtf` baseline exposed a one-neighbor preflight bug: a persistent `\\keepn` chain let the heading remain on page two while its follower and tail moved to page three. Layout now gathers the complete bounded adjacent paragraph chain and preflights its combined height, while preserving the existing behavior for chains taller than a fresh page. The fixture now matches Word's exact three-page grouping; page-three x positions differ by at most about 0.1 points and bundled 24-point Arial-compatible line tops by about 2.5 points. The Word PDF is registered, and both outputs were rendered and visually inspected. Verification passes: 1,136 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Word-compatible incomplete drop-cap recovery (2026-08-03): Microsoft RTF 1.9.1 places `\\dropcapliN`/`\\dropcaptN` inside positioned-frame syntax, and the Word 16.0 baseline confirms that the standalone controls in `drop-cap-passive.rtf` are ignored. Word-compatible mode now requires authored `\\abswN`/`\\abshN` frame dimensions before retaining bounded drop-cap metadata; strict-spec mode preserves the standalone interpretation. The fixture now matches Word's ordinary four-line 12-point flow, with x positions effectively identical and vertical baselines within about 0.8 points. The Word PDF is registered, and both outputs were rendered and visually inspected. Verification passes: 1,135 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Word-compatible stylesheet-reference recovery (2026-08-03): Microsoft RTF 1.9.1 notes that style-number-only streams may not be visually applied unless the concrete formatting is emitted alongside the reference, and the Word 16.0 `stylesheet-semantics-passive.rtf` baseline confirms that recovery behavior for both `\\sN` and `\\csN`. Word-compatible mode now retains style definitions/references as bounded passive metadata while preserving only direct stream formatting; strict-spec mode still resolves inheritance and character-style formatting. `\\snextN` remains editor metadata instead of mutating an already-authored following paragraph. The fixture now matches Word's left/right alignment and Arial/Courier italic selection on one page; residual bundled-font differences are at most about 3.3 points in run width and 1.3 points vertically. The Word PDF is registered, and both outputs were rendered and visually inspected. Verification passes: 1,133 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Word-compatible per-row table geometry refinement (2026-08-03): Microsoft RTF 1.9.1 and Word 16.0 confirm that `\\cellx` boundaries belong to each row and may differ within one table. The safe model now retains bounded per-row width vectors, applies `\\nogrowautofit` without flattening rows to the first width definition, defaults omitted `\\trgaph` to zero, and leaves ordinary tables borderless unless a visible border was authored. In `table-spacing-autofit-passive.rtf`, the formerly displaced authored second cell now starts within 0.11 points of Word (236.00 versus 236.11 points), malformed preferred-width-only rows remain omitted, and no synthetic grid is drawn. The only recorded gap is a symmetric three-point distribution difference for the fixture's non-standard `\\clspdl`/`\\clspdr` aliases. Both PDFs were rendered and visually inspected. Verification passes: 1,132 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Word malformed table-row recovery refinement (2026-08-03): the Word 16.0 `table-spacing-autofit-passive.rtf` baseline proves that ordinary rows lacking authored `\\cellx` boundaries are omitted even when preferred cell/row width metadata is present. Ordinary row normalization now tracks authored boundaries explicitly; preferred widths may refine valid geometry but cannot manufacture a missing table structure. Omission emits a bounded diagnostic and the discarded malformed-row text cannot leak into PDF output. The fixture now has the same six visible text lines as Word; the remaining faint synthetic grid on its two valid borderless rows is recorded as the next table fidelity gap. Both PDFs were rendered and visually inspected. Verification passes: 1,132 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Word-compatible emergency wrapping refinement (2026-08-03): the Word 16.0 `hyphenation-passive.rtf` baseline visibly emergency-wraps both oversized words at character boundaries without drawing hyphen glyphs. The renderer no longer invents dictionary-free line-end punctuation and now splits any unbreakable segment that exceeds the printable width into bounded passive fragments, while authored soft hyphens remain available to materialize at actual break opportunities. The fixture now matches Word's one-page, five-line flow and vertical placement within about 0.8 points. The bundled Arial-compatible metric moves one character from Word's second line to its third (`hmentarian`/`ism` versus `hmentarianis`/`m`); this exact break-point limitation is recorded in the reference policy. Both PDFs were rendered and visually inspected. Verification passes: 1,132 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Page-number restart refinement (2026-08-03): Microsoft RTF 1.9.1 and the Word 16.0 baseline confirm that `\\pgnstartsN` stores a section start value but does not itself restart numbering; continuous numbering is the default and `\\pgnrestart` applies the stored value. The safe page model now retains restart state independently, `\\pgnstartN` remains the document-level initial number, and control order no longer discards a stored section start. `page-number-format-restart-passive.rtf` now visibly matches Word across four pages (`I`, `II`, then restarted decimal `1`, `2`) with matching header placement. The Word PDF is registered in the reference policy and representative Roman/decimal pages were rendered and visually inspected. Verification passes: 1,132 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Facing-page header/footer geometry refinement (2026-08-03): Word 16.0 baselines show that `\\headerl`/`\\footerl` are inactive unless `\\facingp` enables facing pages, while `\\headerr`/`\\footerr` act as the ordinary later-page variants when generic destinations are absent. The safe model now retains `\\facingp` independently from `\\margmirror`, even variants and mirrored margins/gutters activate only for facing pages, headers displace colliding body flow, and `\\footery` anchors the footer bottom rather than its top. `header-footer-variants-passive.rtf` now uses First/Odd content on all three Word-matching pages; `header-footer-distance-passive.rtf` places header/body/footer within about 1.1 points of Word. Both PDFs were rendered and visually inspected. Verification passes: 1,132 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.

- Deterministic native CLI font fallback (2026-08-03): completed Word baselines exposed a real CP437 rendering gap where `font-charset-legacy-code-page-passive.rtf` parsed `\'b3` correctly as U+2502 but emitted `?` in PDF when the requested OEM family was unavailable. Native CLI font loading now appends the bounded bundled metric assets whenever no usable provider exists or requested families remain missing, including `--no-system-fonts`; caller/system assets retain precedence. Both system and no-system PDFs now extract and visibly render `OEM cafe é line │`, matching Word. Verification passes: 1,132 library, 32 CLI, 40 conversion, 2 fuzz, 4 reference-policy, 1,006 security, and 2 WASM-policy tests.
- Standard RTF endnote refinement (2026-08-03): Microsoft RTF 1.9.1 defines endnotes as `\footnote\ftnalt`, with `\fet2` declaring mixed notes. The parser now reclassifies the provisional footnote destination without leaving a ghost footnote/reference count, and treats `\ftnalt` as an inert modifier when `\fet1` already classified the destination as an endnote. The numbering fixture now uses standards-compliant mixed-note syntax and retains its intended `IV`, `V`, and `b` labels on one page. Visual PDF inspection plus 1,131 library and 1,006 security tests pass; the remaining integration targets passed before the monolithic 120-second cap entered security, which was rerun independently.
- Reviewed `docs/project_plan.md` and the current parser/layout/PDF architecture.
- Fixed passive EMF `STRETCHDIBITS` decoding: the parser now reads `iUsageSrc` from its bounded record field and accepts safe DIB/PNG/JPEG raster fallbacks.
- Updated stale formula regression expectations for the already-supported bounded `MIN`, `MAX`, `PRODUCT`, and `ROUND` functions.
- Updated the CLI browser-safe font test to match the intentional bundled wildcard fallback policy.
- Targeted verification: EMF suite (185 tests) and formula suite (26 tests) pass.
- Full-suite verification: core/unit, CLI, conversion, and fuzz suites pass; the broad `security` integration suite still has legacy raw-PDF substring false positives from bundled font streams and a few pre-existing font-encoding assertions.
- PDF hardening checkpoint: embedded TrueType font streams are now Flate-compressed while retaining their uncompressed `Length1`, preventing raw trusted-font bytes from being confused with leaked RTF payloads.
- Security-suite audit: 42 failures remain in legacy assertions that require Base14/Symbol resource names or byte encodings even though current Word-compatible bundled-font output intentionally uses `TF*` Type0 fonts; active-content and parser-boundary checks pass.
- Focused security migration: updated bundled-font coverage, narrow-font scaling, theme-font, and default-font assertions; the full legacy security file now reports 947/1006 passing, with the remaining 59 failures limited to stale byte/resource-name checks, short-token PDF scans, and a few legacy geometry expectations.
- Rendering issues discovered this cycle: none beyond the fixed EMF raster rejection; unsupported paths remain intentionally placeholder-based per the project plan.
- Final verification: `cargo fmt --all -- --check`, 1094 library tests, 39 conversion tests, and 2 fuzz regression tests pass.
- Strict-policy boundary fix: document-info hyperlink metadata is now rejected as active content under `Reject`, while still remaining passive metadata under `Strip`.
- Unicode security migration: assertions now account for supplied passive Type0 fonts and avoid treating embedded font bytes or short control tokens as leaked RTF payloads.
- Passive security suite: all 1006 integration tests pass after the assertion migration; no security-test failures remain.
- Complete verification: 1094 library tests, 39 conversion tests, 2 fuzz regression tests, 1006 security tests, formatting, and `git diff --check` pass.
- Coverage limitation: the reference manifest still contains 177 `pending_word_export` entries without Word-rendered PDFs/PNGs; visual parity against Microsoft Word remains an external follow-up rather than fabricated local evidence.
- Reference-gate hardening: standalone PDF operators now use delimiter-aware markers, and collision-prone two-byte raw scans were removed while retaining longer source/control and active-PDF checks.
- Reference verification: all 3 reference-fixture tests pass across the 177-fixture manifest.
- Full all-features verification: library, CLI, conversion, fuzz, reference, security, WASM dependency-policy, and doc tests pass (`cargo test --all-features -- --test-threads=1`).
- Word-reference contract: manifest entries marked `available` now require an existing parseable PDF with matching page count and required text; all current entries remain explicitly `pending_word_export`.
- Environment audit: LibreOffice is installed and provides a local compatibility reference exporter; Microsoft Word, OnlyOffice, and exact Word PDF/PNG baselines remain unavailable.
- Continuation audit (2026-08-02): split all-feature verification passed: 1094 library tests, 39 conversion tests, 2 fuzz-regression tests, 4 reference-fixture tests, and 1006 security tests. The monolithic all-feature command exceeded the tool's 120-second execution cap while entering the security harness, so the security target was rerun independently and completed successfully in 81.6 seconds.
- Continuation rendering audit: no new issue was observed in representative complex fixtures; the remaining unverified requirement is exact Word visual parity because the manifest still has no locally available Word-exported PDF/PNG baselines.
- Nested-table audit: the former flattening behavior was replaced with a bounded safe-model nested table. Inner rows/cells are retained and their passive text participates in containing-cell layout; nested borders and advanced pagination remain an approximation.
- Nested-table normalization feature: nested table syntax is captured without allowing controls or raw payloads into the model/PDF. The parser emits one bounded approximation warning per document. Focused parser, conversion, reference, and security regression tests pass.
- Nested-table rendering refinement (2026-08-02): normalized nested tables now emit a bounded passive inner grid in the containing cell, with row/column caps and a dedicated layout regression assertion. Full split verification after this change passed library, conversion, fuzz, reference, and WASM policy suites; the security suite was rerun after correcting the test assertion placement and remains green.
- Nested-table formatting refinement (2026-08-02): nested cells now retain bounded passive paragraph and character-run structure, including formatting state, tabs, visible control text, and paragraph breaks. Focused nested parser/security tests and the complete split all-feature suites pass.
- Nested-table geometry refinement (2026-08-02): bounded nested `\\cellx` boundaries now feed normalized inner column widths instead of always forcing equal columns. Focused nested tests plus all library, conversion, fuzz, reference, WASM policy, and security suites pass; nested borders/pagination remain the documented approximation boundary.
- Nested-table border refinement (2026-08-02): nested cell border selectors and bounded border styles/widths/spacing/colors are retained in the safe model and rendered in the inner grid; borderless nested tables suppress synthetic grids when no border controls were authored. Full split all-feature verification remains green.
- Nested-table row geometry refinement (2026-08-02): nested `\\trrh` row-height hints are bounded, retained, and used when distributing inner-grid rows. Focused nested tests and the complete split all-feature suites pass; exact Word visual baselines remain unavailable.
- Nested-table pagination refinement (2026-08-02): split outer-row fragments now carry explicit nested-grid render state, preventing a complete nested grid from being redrawn on every continuation page while passive nested text continues through normal pagination. Added a multi-page regression; all split all-feature suites pass.
- Nested-table continuation audit (2026-08-02): verified the multi-page behavior with a 101-row nested table. The nested grid is emitted only on the first outer-row fragment; continuation pages retain passive text flow without duplicated full-grid geometry. Per-row continuation border placement remains an approximation to Word and is documented for the next fidelity pass.
- Nested-table continuation geometry refinement (2026-08-02): prepared cell lines now retain paragraph provenance, allowing each split fragment to render only the nested row range visible in that fragment. Continuation pages now retain nested border geometry; the multi-page regression and all split all-feature suites pass.
- Nested-cell property refinement (2026-08-02): nested cell shading, bounded padding/spacing, fit-text state, text direction, vertical alignment, and merge metadata are now retained in the safe model; nested shading is rendered passively in the inner grid. Focused model/layout checks and all split all-feature suites pass.
- Nested-cell wrapping and tab geometry refinement (2026-08-02): nested `\\clNoWrap` now maps to bounded paragraph no-wrap state, and authored nested `\\cellx` widths create cumulative passive tab stops for flattened inner-row text. Added parser and layout regressions; exact Word baselines remain unavailable.
- Nested horizontal-merge refinement (2026-08-02): flattened nested rows now use visual merged cells, omit tabs for horizontal-merge continuations, and retain bounded authored tab stops for the remaining cells. Added a merge regression; all split all-feature suites pass.
- Nested merged-grid refinement (2026-08-02): passive nested shading and explicit border geometry now use visual merged-cell spans, avoiding continuation-column fills and internal border lines. Full split all-feature verification remains green; vertical-merge border continuation remains an approximation.
- Nested vertical-merge refinement (2026-08-02): nested vertical-merge first cells now span bounded authored continuation-row heights for passive shading and borders, while continuation cells are not redrawn. Added a geometry regression; all split all-feature suites pass.
- Nested fallback-grid merge refinement (2026-08-02): when nested borders are unavailable, merged rows now use segmented passive grid lines so vertical-merge gaps and horizontal-merge internal boundaries are not drawn through merged cells. Full split all-feature verification remains green.
- Nested cell inset refinement (2026-08-02): bounded nested cell padding/spacing now contributes to the flattened row's initial indent and following tab targets, keeping passive text inside authored inner-cell content areas. Added a layout regression; all split all-feature suites pass.
- Nested paragraph-break refinement (2026-08-02): separate paragraphs captured inside a nested cell now remain visible as bounded passive line breaks when inner rows are flattened for layout. Added a regression; all split all-feature suites pass.
- Nested paragraph-style refinement (2026-08-02): nested paragraph finalization now snapshots the active paragraph style, preserving nested alignment and related passive formatting; visual-cell tab alignment now skips merge continuations consistently. Added a regression; all split all-feature suites pass.
- Nested child-order refinement (2026-08-02): deeper nested-table paragraphs are now deferred until their containing nested row is emitted, preserving passive visual order through bounded recursive flattening. Added a model-level regression; all split all-feature suites pass.
- Nested row-alignment refinement (2026-08-02): nested `\\trql`, `\\trqc`, and `\\trqr` are now retained in the safe model and used to offset explicit passive inner-grid geometry within the containing cell. Added parser/layout regressions; all split all-feature suites pass.
- Nested row-offset refinement (2026-08-02): bounded nested `\\trleft` values now survive normalization and contribute to passive inner-grid placement and flattened row indentation. Added parser coverage; all split all-feature suites pass.
- Nested row-default refinement (2026-08-02): bounded nested `\\trpadd*`, `\\trsp*`, and `\\trgaph` defaults now seed every nested cell while preserving explicit cell overrides; passive flattened insets and tab targets include the authored defaults. Added parser/layout coverage; all split all-feature suites pass.
- Nested row-shading refinement (2026-08-02): bounded nested `\\trcbpat`, `\\trcfpat`, `\\trshdng`, and row shading-pattern controls now seed passive cell shading while explicit `\\clcbpat`/`\\clcfpat` overrides remain local. Added parser/layout coverage; all split all-feature suites pass.
- Nested row-keep refinement (2026-08-02): bounded nested `\\trkeep` and `\\trkeepfollow` flags now survive normalization and map to passive paragraph keep-together/keep-with-next flow hints. Added parser/layout coverage; all split all-feature suites pass.
- Nested RTL refinement (2026-08-02): bounded nested `\\rtlrow`/`\\taprtl` state now reverses passive inner-cell order, with authored column widths reversed for uniformly RTL nested tables. Added parser/layout coverage; all split all-feature suites pass.
- Nested preferred-width refinement (2026-08-02): bounded nested `\\clftsWidth`/`\\clwWidth` values now provide passive column widths when `\\cellx` boundaries are absent, using the existing twips/percentage normalization and safe fallback widths. Added layout coverage; all split all-feature suites pass.
- Nested preferred-row-width refinement (2026-08-02): bounded nested `\\trftsWidth`/`\\trwWidth` totals now distribute across inner columns when cell boundaries and cell preferred widths are absent. Added layout coverage; all split all-feature suites pass.
- Nested repeat-header metadata refinement (2026-08-02): bounded nested `\\trhdr` state now survives normalization in passive row metadata, keeping header intent available to continuation rendering. Added parser/layout coverage; all split all-feature suites pass.
- Nested repeat-header rendering refinement (2026-08-02): cached passive lines from leading nested `\\trhdr` rows are replayed on split containing-row fragments, so nested headers remain visible on continuation pages without reintroducing raw RTF state. Added multi-page layout coverage; all split all-feature suites pass.
- Nested vertical-merge continuation refinement (2026-08-02): bounded merge-origin lookup now carries nested shading and side-border geometry onto visible continuation segments when a containing row splits across pages. Added multi-page layout coverage; all split all-feature suites pass.
- Nested vertical-merge boundary refinement (2026-08-02): visible continuation segments now close their inherited bottom border only when the merge actually ends, avoiding open boxes or premature internal rules across page fragments. Extended the split regression; all split all-feature suites pass.
- Nested vertical-merge closure refinement (2026-08-02): inherited bottom borders now close on the final visible continuation row while remaining suppressed across internal continuation boundaries. Extended the split regression with explicit border coverage; all split all-feature suites pass.
- Nested homogeneous text-direction refinement (2026-08-02): safe optional paragraph direction metadata now preserves homogeneous vertical text direction from nested cells through measurement, row splitting, and passive rotation; mixed-direction rows retain the bounded approximation. Added layout coverage; all split all-feature suites pass.
- Nested authored-row-height refinement (2026-08-02): safe optional nested row-height metadata now contributes bounded passive minimum spacing to flattened containing-cell flow, keeping `\\trrh` visible in both inner geometry and text pagination. Added layout coverage; all split all-feature suites pass.
- Nested authored-row-alignment refinement (2026-08-02): homogeneous nested `\\clvertalc`/`\\clvertalb` hints now distribute bounded extra `\\trrh` space before/after flattened text, preserving passive center/bottom alignment while mixed rows retain the approximation. Added layout coverage; all split all-feature suites pass.
- Table row non-splitting refinement (2026-08-02): ordinary and nested `\\trcantSplit` controls now map to bounded passive keep-together metadata, preventing eligible rows from being split across pages through the existing pagination path. Added parser coverage; focused pagination tests pass.
- Nested protected-fragment refinement (2026-08-02): nested rows carrying passive keep-together metadata are now kept as whole paragraph groups during tall containing-row fragmentation whenever the group fits the available page fragment; oversized groups still degrade boundedly. Added split-boundary coverage; 1117 library, 39 conversion, 2 fuzz, 4 reference, 2 WASM-policy, and 1006 security tests pass.
- Nested inner-border refinement (2026-08-02): nested `\\trbrdrh` and `\\trbrdrv` controls now retain bounded passive horizontal/vertical border strokes and apply them to inner-grid cell edges without overriding explicit cell borders. Added parser coverage; 1118 library, 39 conversion, 2 fuzz, 4 reference, 2 WASM-policy, and 1006 security tests pass.
- Nested outer-border refinement (2026-08-02): nested `\\trbrdrl`, `\\trbrdrr`, `\\trbrdrt`, and `\\trbrdrb` controls now retain bounded passive outer strokes on the corresponding inner-grid edges, with explicit cell borders taking precedence. Added parser coverage; 1118 library, 39 conversion, 2 fuzz, 4 reference, 2 WASM-policy, and 1006 security tests pass.
- Nested keep-follow refinement (2026-08-02): nested rows carrying passive `\\trkeepfollow` metadata now keep the current and immediately following paragraph groups together during tall containing-row fragmentation whenever they fit the available fragment. Added split-boundary coverage; 1119 library, 39 conversion, 2 fuzz, 4 reference, 2 WASM-policy, and 1006 security tests pass.
- Nested RTL metadata refinement (2026-08-02): RTL nested rows now reverse per-cell borders, shading, padding, alignment, merge, and text-direction metadata alongside passive visual cell order, preventing authored-index properties from landing on the wrong visible cell. Added layout coverage; 1119 library, 39 conversion, 2 fuzz, 4 reference, 2 WASM-policy, and 1006 security tests pass.
- Deeper nested-table recovery refinement (2026-08-02): level-3-and-deeper table structure is now consumed as bounded passive structure instead of being misinterpreted as sibling rows/cells in the level-2 approximation; visible deeper text remains in the containing nested cell. Added parser coverage; 1120 library, 39 conversion, 2 fuzz, 4 reference, 2 WASM-policy, and 1006 security tests pass, with formatting and diff checks clean.
- Current rendering limitations (2026-08-02): exact Word PDF/PNG parity remains unverified because no Word-compatible reference exporter or baseline artifacts are present; mixed-direction nested rows and child-grid row-range behavior across a split containing row remain bounded passive approximations even though deeper visible text and geometry are retained safely. Documentation tests also pass (0 doctests).
- Nested continuation-border refinement (2026-08-02): nested fallback-grid selection now considers the complete bounded table, so borderless continuation fragments no longer synthesize 0.25-point grid lines merely because authored borders occur on another row. Added a multi-page regression; 1121 library tests pass.
- Post-commit verification (2026-08-02): commit `1b6dd6de` passes the conversion (39), reference (4), and security (1006) suites in addition to the 1121 library tests; formatting and diff checks remain clean.
- Recursive nested-grid refinement (2026-08-02): passive nested-table grid emission now recurses through model-retained child tables with bounded child insets and an eight-level depth cap, so deeper nested cells receive their own shading/border grid geometry instead of text-only flattening. Extended the recursive child-order regression; 1121 library tests pass.
- Recursive-grid validation (2026-08-02): the recursive nested-grid change passes the conversion (39) and security (1006) suites in addition to the 1121 library tests; formatting and diff checks remain clean. Exact Word geometry for recursive continuation fragments remains unverified without Word baselines.
- Recursive continuation refinement (2026-08-02): split containing-row fragments now render descendant child grids only on the first fragment while retaining the current nested table’s visible continuation grid. Added a direct fragment regression; 1121 library tests pass.
- LibreOffice reference audit (2026-08-02): LibreOffice is installed and generated local PDF baselines for the core, table, nested-table, and complex sample fixtures. Comparison exposed two concrete nested-table gaps: authored inline ordering and synthetic borders on borderless nested tables. The renderer now anchors normalized nested content at its source position and retains nested border visibility, suppressing synthetic grids when no border controls were authored. The remaining nested fixture difference is LibreOffice's inline/floating placement of the deliberately mixed nested text; it remains a bounded approximation pending broader fixture comparison.
- LibreOffice table audit (2026-08-02): representative table baselines confirm matching core geometry for ordinary, merged, bordered, padded, repeated-header, and row-height cases. Remaining visual differences include mixed-direction cell placement, small merged-cell text spacing, and LibreOffice's fallback treatment of an oversized split row; these stay within the renderer's documented passive approximation boundary and were not changed speculatively.
- Vertical table-text refinement (2026-08-02): vertical cell paragraphs now use a bounded generous wrap width before passive rotation, preventing ordinary mixed-direction runs from wrapping at the narrow horizontal cell width. Added a fixture-backed layout regression; conversion (39), security (1006), reference (4), formatting, and diff checks pass. LibreOffice comparison still shows its distinct inline placement for mixed-direction cells as a bounded compatibility difference.
- Recursive nested-grid range refinement (2026-08-02): parent nested-grid row ranges now ignore deferred descendant paragraphs beyond the parent table's bounded row count, preventing child content from expanding the parent's continuation grid range. Added a recursive layout regression; 1123 library, 39 conversion, 1006 security, and 4 reference tests pass with formatting and diff checks clean.
- Nested anchor-range refinement (2026-08-02): nested-grid row-range discovery now honors the authored anchor position after earlier paragraphs in the containing cell, rather than assuming every nested table begins after the original paragraph list. Added an anchored multi-paragraph regression; 1123 library, 39 conversion, 1006 security, and 4 reference tests pass.
- Nested anchor-range verification (2026-08-02): the anchored range fix passes the complete 1124-test library suite, conversion (39), security (1006), reference (4), formatting, and diff checks. Exact Word continuation geometry remains unverified without Word-rendered baselines.
- Complex-sample visual audit (2026-08-02): inspected the current two-page sample render after the nested-grid refinements; table borders, merged-looking empty cells, list flow, alignment sections, passive shapes, and multilingual text remain visibly coherent. No additional actionable issue was isolated beyond the already documented Word-reference and nested mixed-direction approximation gaps.
- Section-number audit (2026-08-02): LibreOffice emits three pages for `section-number-passive.rtf`, while the converter emits the manifest's expected two pages because `\sbknone\sect` is normalized as a continuous section break. The parser and layout tests explicitly cover this mapping; the discrepancy is recorded as LibreOffice recovery behavior rather than adopted without Word evidence.
- Full LibreOffice corpus audit (2026-08-02): LibreOffice and the converter both produced PDFs for all 176 top-level RTF fixtures. Normalized text differed on 71 fixtures and extracted page counts differed on 11; the largest classes are intentional active-content/security differences, metadata or object extraction-order differences, and LibreOffice-specific table/header or malformed-input recovery behavior. No new renderer change was made from those indirect comparisons without a Word-backed visual confirmation; the representative table and nested-table findings remain the actionable compatibility evidence.
- First Word 365 reference baseline (2026-08-03): Word 16.0 manually exported `active-control-fields-passive.rtf` as a one-page 306x396-point PDF, now registered under `fixtures/reference/word365`. Visual and extracted-text inspection confirms Word renders resultless active form/control fields as inert bookmark-error text, while the converter intentionally emits bounded passive placeholders and no PDF forms/actions. The manifest now supports Word-specific expected text for intentional security differences.
- Word default-font refinement (2026-08-03): the first Word baseline showed that fontless legacy RTF uses Times New Roman rather than the converter's synthetic Helvetica default. The RTF parser now seeds font index 0 as Times New Roman/Roman and layout falls back to Times when no usable font definition exists. The corrected output matches Word's serif default while retaining the documented active-field placeholder difference; 1126 library, 39 conversion, 1006 security, and 4 reference tests pass.
- Word exporter environment note (2026-08-03): Word can open and manually save the fixtures as PDF, but both late-bound COM `ExportAsFixedFormat` and `SaveAs2` calls hang in the current desktop PowerShell workflow. Broad baseline generation remains pending a reliable isolated automation path; the first manually exported reference is valid and executable in the reference harness.
- Word exporter resolution (2026-08-03): replaced the late-bound PowerShell COM prototype with a typed C# STA wrapper over the installed Microsoft Office interop assemblies. A user-run proof exported `alignment-direction-passive.rtf` successfully. The batch helper disables Office macro automation and link updates, validates non-empty PDF output, supports fixture/limit filters, and resumes by skipping existing PDFs.
- Full Word 365 batch audit (2026-08-03): the automated exporter produced 171 valid PDFs from 176 top-level fixtures; Word rejected five fixtures (`carried-table-row-definitions`, `font-template-metadata`, `generated-reference-fields`, `mailmerge-object-metadata`, and `wmf-srccopy-cropped-jpeg-dib`) with corruption/resource/export errors. Across successful pairs, 101 normalized texts matched exactly, 70 differed, seven page counts differed, and three fixtures exposed later-section page-size drift.
- Section-default geometry refinement (2026-08-03): Word baselines proved that `\sectd` resets section-local formatting without discarding document-level paper size, margins, gutter, header/footer distance, or orientation. The parser now rebuilds section defaults from those authored document physical-page values. `line-numbering-passive`, `page-number-format-restart-passive`, and `section-grid-page-number-passive` now retain Word's 306x396-point geometry on every page; a parser regression covers both initial and later `\sectd` use.
- Section-default geometry verification (2026-08-03): 1127 library, 39 conversion, 1006 security, and 4 reference tests pass after the Word-backed `\sectd` refinement.
- Word line-spacing recovery refinement (2026-08-03): the Word baseline shows that malformed extreme `\sl99999` falls back to ordinary automatic line spacing rather than a large bounded line height. Out-of-policy line-spacing values now recover to automatic spacing with a diagnostic instead of clamping to the largest permitted value. `line-spacing-passive.rtf` now renders on one page like Word; the final two paragraph positions are within 0.3 points vertically of the Word PDF. A parser limit regression and fixture-level page-count regression cover the behavior.
- Word note-flow refinement (2026-08-03): section/document endnotes now remain in body flow before the final footnote band instead of being forced onto a separate page. Final footnote materialization is deferred at immediate section/document boundaries while normal page-overflow flushing remains active. `note-placement-passive.rtf` now matches Word's two pages and `note-restart-numbering-passive.rtf` matches Word's one page. Visual inspection confirms passive note text remains legible and separated; numbering-control precedence and intentional object placeholders remain explicit follow-up differences.
- Word malformed-margin recovery refinement (2026-08-03): Word ignores the oversized `\margt720720`/`\margb720720` controls in `hyphenation-passive.rtf` and uses 1,440-twip vertical defaults. Oversized margins now recover to bounded defaults with diagnostics instead of clamping to page-consuming maximums. The fixture now renders one page at Word's x=36/y≈72-point origin instead of five blank pages. Remaining difference: Word emergency-wraps the narrow-page words without visible hyphens, while the converter's bounded hyphenation emits explicit line-end hyphens.
- Word-compatible malformed legacy-polyline suppression (2026-08-03): the completed Word 16.0 `shape-line-join-passive.rtf` baseline contains no drawing because the fixture omits the required `\dppolycountN` vertex declaration. Legacy `\do` polylines and polygons now require a declared count matching the bounded parsed point list in Word-compatible mode; malformed primitives are silently omitted with a diagnostic, while StrictSpec retains bounded recovery and legacy arc presets remain independent. The corrected and Word PDFs both contain zero drawings/images and identical two-line flow, with only the existing roughly 0.96-point bundled Arial-compatible vertical residual. Both PDFs were rendered and visually inspected. Verification passes: 1,164 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,009 security, and 2 WASM-policy tests (2,253 total); formatting and `git diff --check` pass.
- Word modern-shape rotation and wrap refinement (2026-08-03): the Word 16.0 `shape-rotation-passive.rtf` baseline proves OfficeArt `rotation` uses 16.16 fixed degrees (`2700000 / 65536 = 41.1987 degrees`), not DrawingML's 1/60000-degree unit. Modern shapes now rotate into bounded passive polygon geometry with Word-matching points, default to Word-compatible text wrapping with nine-point horizontal wrap margins, retain their empty anchor paragraph, and allow simple left-aligned body text to flow through both same-baseline intervals around the exclusion. The converter and Word now draw identical geometry and place the wrapped line within about 0.8 points vertically; the right segment starts 1.55 points left of Word due to the remaining font/exclusion metric residual. Legacy `\do` wrapping and StrictSpec behavior remain unchanged. Visual PDFs/PNGs were inspected. Verification passes: 1,164 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,010 security, and 2 WASM-policy tests after updating the modern shape-picture default-margin regression; formatting and `git diff --check` pass.
- Bounded inline-picture paragraph flow (2026-08-03): ordinary body `\pict` groups that follow paragraph text now retain their decoded safe `Block::Image` for security/model inspection while a bounded private reference places the same image inside paragraph wrapping. The renderer measures image width/height, bottom-aligns it to the Word inline-object baseline, skips the referenced block's separate layout pass, and keeps an inline image plus directly adjacent no-whitespace text as one line-breaking cluster. `wmf-text-passive.rtf` now matches Word's 72-by-36-point image slot and surrounding/wrapped text within 0.23 points horizontally and 0.02 points vertically; `wmf-vector-passive.rtf` now matches Word's three-line text/image flow and text origins within 0.16 points. Raw metafile bytes remain absent from the normalized/PDF layers, the decoded image resource remains bounded by the existing image limits, and the established image-block security assertions remain valid. Remaining isolated gaps are WMF font selection/clipping (`Hi` versus Word's clipped bold `H`) and WMF map-mode/stock-pen geometry. Visual PDFs/PNGs were inspected. Verification passes: 1,164 library, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,011 security (full run had one stale absolute inline-position expectation, corrected and rerun targeted), and 2 WASM-policy tests; formatting and `git diff --check` pass.
- Word WMF stock-font recovery (2026-08-03): fontless WMF EXTTEXTOUT now models Word's stock SYSTEM_FONT as a bounded bold Arial-compatible face, derives the Word-observed 6.52-point display scale, preserves GDI's initial opaque white background, and applies the frame-sized stock advance only when no authored character or word spacing exists. The PDF resource collector now registers Helvetica-Bold when vector text requests it, eliminating an invalid missing `/F2` resource and viewer-dependent substitution. `wmf-text-passive.rtf` visibly extracts only the clipped bold `H`, like Word; its x/top starts within 0.24/0.02 points, with a remaining roughly 1.69-point lower glyph-box residual from Base14 Helvetica-Bold versus Word's embedded Arial Bold. Raw WMF bytes remain outside the model/PDF boundary and all recovery growth is clipped to the passive image frame. Visual Word/current PNGs were inspected. Verification passes: 1,164 library, 1,011 security, 40 conversion, 2 fuzz-regression, 4 reference-policy, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word terminal no-wrap row recovery (2026-08-03): a final authored-height table row containing no-wrap cell text now remains in the available page tail when its measured height crosses the physical page boundary, is clipped by the passive PDF page/cell bounds, and suppresses subsequent body flow until an explicit page, column, or section boundary. This narrowly models Word's `no-wrap-passive.rtf` recovery without changing ordinary table pagination. The fixture now matches Word's single 216-by-216-point page instead of moving the row and trailing paragraph to page two; its table begins within about 1.1 points vertically of Word. Remaining line-break differences are the broader bundled Arial metric residual. The normalized model still retains the trailing paragraph, while only its out-of-page visual flow is suppressed. Visual Word/current PNGs were inspected. Verification passes: 1,165 library, 1,011 security, 40 conversion, 2 fuzz-regression, 4 reference-policy, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word floating-table anchor and exclusion flow (2026-08-03): positioned floating rows now interpret `\posx`/`\posy` from the authored page-margin anchor, subtract the floating row gap from the physical table origin, keep wrap distance outside rather than shifting the table, and independently place consecutive positioned rows at the same Word origin. A bounded one-block lookahead registers an immediately following floating exclusion while wrapping the preceding paragraph; dynamic emergency wrapping splits only an ordinary word that exceeds the active narrowed interval. `floating-table-positioning.rtf` now has Word-identical visible line breaks (`Bef`/`ore`/`floating table.`), overlapping table rows, and one-line following body text. Table/body origins are within about 0.86 points vertically and 0.08 points horizontally of Word, leaving only bundled Arial-compatible metric drift. The preview exclusion is removed before table placement and replaced by the table's normal passive exclusion, preventing self-collision with `\tabsnoovrlp`. Raw positioning controls remain absent from PDF output. Visual Word/current PNGs were inspected. Verification passes: 1,166 library, 1,011 security, 40 conversion, 2 fuzz-regression, 4 reference-policy, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Completed Word baseline export (2026-08-03): the resumable batch contains 171 valid PDFs from 176 fixtures. Missing outputs are `carried-table-row-definitions-passive` (Word reports a corrupted table), `font-template-metadata-passive`, `generated-reference-fields-passive`, `mailmerge-object-metadata-passive`, and `wmf-srccopy-cropped-jpeg-dib-passive`; these remain explicit Word export rejections rather than silently fabricated references.
- Word character-shading refinement (2026-08-03): the `character-highlight-border-passive.rtf` baseline proves `\chshdng5000` scales the selected character background toward black, producing half-intensity green (and half-intensity automatic black as gray), rather than blending toward white. Character shading now uses that Word-compatible interpretation without changing paragraph/table shading semantics; the focused regression passes. Character-border grouping, border-reserved line metrics, and full-line highlight height remain the next focused gaps.
- Word character-border box and shading completion (2026-08-03): character-bordered authored runs now remain a single bounded inline box, reserve the full clamped stroke/spacing inset on both sides, expand natural line height by the top and bottom border widths, and use Word's asymmetric baseline placement inside the expanded box. Solid borders emit four passive filled rectangles, eliminating center-stroke corner protrusions while non-solid styles retain bounded line semantics. A decorated line expands neighboring non-bordered highlight/shading fills to the complete line box; bordered text keeps shading inside the border. `\chcbpat0` now retains Word's automatic-color state so a persisted `\chshdng5000` renders neutral 50% gray instead of disappearing. Visual comparison of `character-highlight-border-passive.rtf` shows matching 12-point border geometry and 37.8-point decorated bands; horizontal origins differ by at most roughly 0.25 points and vertical text origins by the established roughly 0.96-point Base14 Times residual. Late NUMPAGES/SECTIONPAGES border rectangles remain width-adjustable. Verification passes: 1,166 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word print suppression for form-field shading (2026-08-03): the Word 16.0 `form-field-shading-passive.rtf` PDF contains zero drawing objects, proving `\formshade` is an editor display aid rather than printed decoration. Word-compatible parsing now emits a diagnostic and omits the shading style while preserving the safe stored result as ordinary passive text; StrictSpec retains the prior bounded gray fill behavior. Regenerated Word/current PDFs have identical line breaks and zero drawings, with horizontal origins within 0.05 points and only the established roughly 0.95-point bundled Arial vertical residual. Form metadata, macros, data payloads, PDF widgets, actions, and active forms remain stripped. Verification passes: 1,166 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word print suppression for legacy form checkboxes (2026-08-03): the Word 16.0 `form-checkbox-passive.rtf` PDF contains neither checkbox glyphs nor drawing objects for checked or unchecked FORMCHECKBOX fields; it preserves only the authored spaces around each blank editor-control position. Word-compatible parsing now emits an explicit editor-only diagnostic and produces the same blank inline result, removing vector-induced line-height growth. StrictSpec retains the bounded checked/unchecked passive vector fallback and size metadata. Regenerated Word/current PDFs have identical four-line flow and zero drawings; converter baselines remain roughly 0.83 points above Word from bundled Times-compatible metrics. Form names, macro hooks, data payloads, ZapfDingbats dependencies, widgets, actions, and active forms remain absent from the PDF. Verification passes: 1,166 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word print handling for FORMTEXT and FORMDROPDOWN controls (2026-08-03): the Word 16.0 `form-text-dropdown-passive.rtf` reference prints the stored FORMTEXT `\fldrslt` but leaves resultless FORMTEXT defaults, empty text controls, and FORMDROPDOWN selections blank. Word-compatible parsing now preserves that five-line text flow without synthesizing defaults, selected entries, or removal placeholders; StrictSpec retains bounded passive form defaults and dropdown selections. Regenerated Word/current PDFs have the same five lines and zero drawings, with converter baselines roughly 0.83 points above Word from bundled Times-compatible metrics. Hidden names, macro hooks, data payloads, interactive widgets, actions, and external links remain stripped. Verification passes: 1,166 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word low-ANSI associated-character formatting (2026-08-03): `associated-character-passive.rtf` proves Word applies `\ab`, `\ai`, `\acf1`, and `\aul` to the visible low-ANSI run but does not apply associated expansion `\aexpnd4` or baseline raise `\aup6` after `\loch`. Parser state now tracks low/high/double-byte selectors; Word-compatible low-ANSI associated expansion and vertical shifts are ignored while StrictSpec retains their bounded values. Word's associated underline is a 1.2-point band; Word-compatible `\aul` now maps to the thick passive underline and uses a calibrated 2.8-point baseline offset. The corrected styled run ends within 0.08 points of Word and its text baseline remains within the established roughly 0.96-point Arial-compatible residual. The default active-content policy intentionally adds `[Embedded object removed]` for the adjacent malformed object that Word silently omits, so following flow differs by one line as a documented security choice. Verification passes: 1,166 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Focused zero-width formatting audit (2026-08-03): Word 16.0 and the converter both render `zero-width-formatting-passive.rtf` as the same three visible lines, with the six `\zwbo`, `\zwnbo`, `\zwnj`, `\zwj`, `\ltrmark`, and `\rtlmark` controls contributing no glyphs, drawings, or spacing between `ABCDELR`. PDF extraction likewise contains only the adjacent visible letters; no control syntax or invisible directional character leaks into selectable text. Word includes a trailing paragraph space in its extraction while the converter does not, which has no visible effect and is unrelated to the zero-width controls. Both PDFs were rendered and visually inspected; no parser change was needed. Remaining sub-point width and baseline differences are the established bundled Times-compatible metric residual.
- Focused Symbol-font decoding audit (2026-08-03): Word 16.0 and the converter visibly render the same bullet, less-than-or-equal, plus/minus, and square-root sequence in `symbol-font-passive.rtf`, with matching line flow and no drawings. Word's embedded Symbol font extracts the final three glyphs as private-use `U+F0A3`, `U+F0B1`, and `U+F0D6`; the converter intentionally normalizes them to semantic `U+2264`, `U+00B1`, and `U+221A`, preserving meaningful copy/paste without altering passive appearance. Raw font-table controls and source hex escapes remain outside the PDF. Both PDFs were rendered and visually inspected; no renderer change was needed. Arial-compatible line origins remain within the established roughly one-point vertical metric residual.
- Focused resultless SYMBOL-field audit (2026-08-03): Word 16.0 and the converter both render `SYMBOL 65` as the ordinary inline letter `A` in `symbol-field-passive.rtf`, preserving the same three-line flow with no drawings. The converter's PDF extraction contains only the visible sentence; `\field`, `\fldinst`, and `SYMBOL` metadata do not cross into the PDF layer. Both PDFs were rendered and geometrically inspected; no parser change was needed. Horizontal origins differ by at most 0.03 points and vertical origins retain the established roughly 0.95-point Arial-compatible metric residual.
- Word-compatible paired legacy-list suppression (2026-08-03): the registered Word 16.0 `pntext-symbol-list-passive.rtf` reference suppresses both the explicit Symbol bullet / Roman `\pntext` markers and the paired ignorable `\pn` compatibility metadata, including their authored 18/36-point hanging indents. The converter formerly printed separate bullet and `I.` marker runs and indented both item bodies. Word-compatible parsing now recognizes only the narrow paired `\pntext` followed by ignorable `\pn` structure, clears its pending marker, and ignores its paragraph `\li`/`\fi` controls through the item boundary. Standalone bounded `\pntext` recovery is unchanged; StrictSpec explicitly retains the marker and authored hanging indent. The corrected PDF has Word-identical four-line flow and 36-point left origins, with the established roughly 0.95-point Arial-compatible vertical residual. Raw list/font metadata remains outside the PDF. Both PDFs were rendered and visually inspected. Verification passes: 1,167 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word legacy dingbat source-font advances (2026-08-03): focused Word 16.0 audits measured nonstandard spacing retained by Wingdings-family glyph runs after safe Unicode/vector normalization. Wingdings, Wingdings 2, and Wingdings 3 spaces are full-em (12 points at `\fs24`); Webdings spaces are half-em. Source glyph advances also differ materially from Base14 Symbol/Zapf estimates. Layout now applies bounded source-family advance tables while PDF output remains semantic Unicode or passive vector geometry and raw font metadata remains stripped. `wingdings3-arrows-passive.rtf` now reproduces Word's separated arrow sequence, with the final down-arrow origin/end within roughly 0.13 points and the field arrow unchanged. Wingdings/Wingdings 2/Webdings checkbox sequence positions are corrected, while visual audit records remaining exact vector-shape and line-height gaps (including Wingdings `0xA3`, which Word prints as a bullseye rather than an empty box). Word/current PDFs for all four fixtures were rendered and visually inspected. Verification passes: 1,168 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word Wingdings bullseye recovery (2026-08-03): the registered Word `wingdings-checkbox-passive.rtf` reference proves source byte `0xA3` is a filled bullseye/ring, not the empty checkbox previously produced by the generic Wingdings mapping. Parsing now normalizes it to semantic `U+25C9`; passive layout treats that character as a bounded dingbat fallback with Word's measured 10.704-point advance, and PDF emission draws an even-odd filled ring without embedding Wingdings or relying on viewer fonts. The corrected outer bounds are 10.68 points at `\fs24`, matching Word's roughly 10.69-point glyph box, and the sequence spacing remains calibrated. Word/current PDFs were rendered and visually inspected. Fine check/X geometry and the checkbox-line baseline interval remain explicit follow-up gaps. Verification passes: 1,168 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Provider-independent passive checkbox line metrics (2026-08-04): installed Wingdings metrics formerly expanded the resultless SYMBOL-field line to 15.42 points while the browser-safe fallback used 13.8 points. Layout now gives semantic passive checkbox vectors the bounded Base14 1.15 line-height multiplier before consulting any supplied or system font, and caps mixed checkbox lines to the same metric. Normal system-font and browser-safe conversions now both retain Word's exact 13.8-point baseline intervals across `wingdings-checkbox-passive.rtf`; Wingdings 2 and Webdings retain the same four-line rhythm, and Segoe UI Symbol remains bounded. Word/current pages were rendered at 2x and visually inspected; the converter retains its established roughly 0.95-point Arial-compatible page-origin residual. Fine check/X vector contours remain a separate geometry gap. Verification passes: 1,169 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word Webdings inbox recovery (2026-08-04): the registered `webdings-checkbox-passive.rtf` baseline proves Webdings source byte `0x3F` is an incoming-paper/inbox-tray icon, not the ballot box with X produced by the prior generic mapping. Parsing now normalizes it to semantic `U+1F4E5`, retains the measured full-em Webdings advance, and emits bounded passive tray/paper vector geometry without embedding or depending on Webdings. Direct and resultless SYMBOL-field forms use the same safe path; raw field instructions and source-font metadata remain outside the PDF. Word/current pages were rendered at 4x and visually inspected; four-line rhythm and horizontal sequence placement remain matched, with only minor contour differences recorded. Verification passes: 1,169 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word occupied-content column separators (2026-08-04): focused comparison of `section-columns-passive.rtf` confirms that Word draws `\linebetcol` only across the section's occupied content band, not through the full page body. The former converter rule was a 0.5-point gray line spanning 324 points; separator layout now derives bounded body-item extents, uses the line box around visible text, and emits Word's black 0.72-point rule across exactly 13.8 points. Explicit 90-point column widths, 18-point gap, column-break flow, and horizontal text origins already match within 0.13 points. Word/current pages were rendered at 4x and visually inspected; rule endpoints retain only the established roughly 0.95-point Arial-compatible page-origin residual. Verification passes: 1,170 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Deterministic Word-compatible resultless timestamp/stat fields (2026-08-04): the registered `timestamp-stat-fields-passive.rtf` baseline proves Word leaves resultless CREATEDATE, EDITTIME, and REVNUM positions blank despite authored `\creatim`, `\edmins`, and `\version` metadata, while SAVEDATE, PRINTDATE, DATE, TIME, and malformed picture tokens are dynamically expanded from Word's host/filesystem state. Word-compatible passive mode now omits all seven resultless field kinds without reading the clock, file metadata, or executing field instructions; this restores Word's six-line flow and removes three synthetic placeholder lines. StrictSpec retains bounded metadata-derived values and placeholder recovery. Hidden active metadata remains stripped, and field instructions never reach PDF output. Word/current pages were rendered at 2x and visually inspected; deterministic omissions are recorded as intentional security differences. Verification passes: 1,170 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word WMF stock-pen playback width (2026-08-04): Word 16.0 emits the fresh GDI BLACK_PEN as a consistent 1.87784-point PDF stroke across the rectangle/ellipse, line, polyline, polygon, and clipped-rectangle fixtures; the converter formerly scaled its one-logical-unit default to 0.45 points. The bounded WMF drawing state now uses the measured 4.172978-logical-unit stock width, which maps to the exact Word PDF width in the authored 200-by-100 to 108-by-36-point frame. Explicit CREATEPEN selections still replace the stock state and retain their authored bounded widths. Word/current vector and poly-path pages were rendered at 4x and visually inspected. Remaining coordinate differences are isolated to Word's WMF playback/map transform rather than pen weight. Verification passes: 1,171 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word paragraph-anchored static-shape flow (2026-08-04): `static-shape-wrap.rtf` and `shape-wrap-distance-passive.rtf` proved that `\shpbypara` positions the shape after the preceding paragraph's consumed line box, not at that paragraph's top/body origin. Layout now records the post-paragraph cursor as the bounded paragraph anchor, moving both 108-by-36-point frames from the overlapping y=36 origin to Word's exact y=49.8-85.8 band. Word also permits the final glyph box in a right-of-floating-object interval to overhang the nominal body edge by up to about 2.8 points; a bounded 3-point tolerance now applies only to a right-side exclusion interval ending at the content edge, leaving ordinary lines and interior object gaps unchanged. Both fixtures now have Word-identical six/eight-line flow and exact 9/36/0/18-point authored wrap-distance behavior. Word/current pages were rendered at 4x and visually inspected; only the established roughly 0.95-point bundled Arial-compatible glyph-box residual remains. Verification passes: 1,172 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word WMF authored dashed-pen width and inline edge flow (2026-08-04): focused inspection of `wmf-dashed-pen-passive.rtf` measured Word's four-logical-unit CREATEPEN as a 1.8778403-point red stroke with exact `[5.6335208 1.8778403]` 3:1 dash cadence. Bounded authored WMF pen widths now use the same measured playback scale as Word's stock pen, producing 1.8778400 points and `[5.63352 1.87784]` while preserving style, phase, color, and passive-only vector output. Word also permits trailing text on a body-width line containing an inline image to overhang the nominal edge by 0.802 points; a bounded one-point tolerance now applies only to such lines. The fixture consequently has Word-identical four-line grouping, including `After WMF dashed` beside the image. Word/current pages were rendered at 4x and visually inspected. The remaining isolated WMF coordinate transform differs by about 0.02 points at the start, 1.10 points at the endpoint, and 0.70 points vertically. Verification passes: 1,173 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word modern-shape default anchor/fill recovery (2026-08-04): the registered OfficeArt flag/color fixtures consistently place shapes at `(108,108)-(216,144)` when no `\shpby*` control is authored, proving Word's modern-shape default vertical reference is the page margin rather than the preceding paragraph. Modern `\shp` parsing now defaults to margin anchoring while legacy `\do` retains paragraph anchoring and explicit `\shpbypara`, `\shpbymargin`, and `\shpbypage` remain authoritative; the explicit paragraph-anchor wrap fixtures remain unchanged. Word also defaults an ordinary modern rectangle to opaque white fill, so `fLine=0` now retains its exact white 108-by-36-point fill instead of disappearing. Focused audits confirm exact frame geometry across disabled fill/line, last-state fill/line flags, foreground/background colors, flip, pattern, gradient, and shadow fixtures. Disabled fill retains only the black 0.75-point outline; disabled line retains only white fill; final enabled flags preserve authored yellow/default white fill and black outline. Invalid authored fill colors are stripped and recover to the same safe white default rather than attacker-selected black. Word/current disabled-line pages were rendered at 4x and visually inspected. Verification passes: 1,174 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word modern-shape default shadow offset (2026-08-04): `shape-shadow-opacity-passive.rtf` contains no authored offset, and Word paints its 50% gray shadow at `(110,110)-(218,146)`, exactly two points down/right from the `(108,108)-(216,144)` shape. The modern OfficeArt default is now 40 twips on each axis instead of 60; explicit bounded `shadowOffsetX`/`shadowOffsetY` EMU properties continue to override it, including negative offsets. Current and Word drawing bounds, color, opacity, and shadow-before-shape paint order now match; the PDF alpha values differ only by Word's 32768/65535 representation versus the converter's exact 50%. Both pages were rendered at 4x and visually inspected. Verification passes: 1,175 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word textual OfficeArt fill-type recovery (2026-08-04): focused Word 16.0 comparison proves that textual `msoFillPattern` and `msofillShade` property values are inert in Word print output. Word paints the pattern fixture as a plain solid green rectangle and the gradient fixture as a plain solid red rectangle, ignoring the authored blue back color; both retain the exact `(108,108)-(216,144)` frame and default black 0.75-point outline. Word-compatible parsing now ignores nonnumeric fill-type aliases with an explicit diagnostic, and only promotes a back color into the model when a numeric gradient type was actually selected. Numeric fill types retain bounded passive hatch/gradient support, while StrictSpec keeps textual alias recovery. Word/current gradient pages were rendered at 6x and visually inspected; visible geometry and color now match exactly. Verification passes: 1,176 library, 40 conversion, 4 reference-policy, 1,011 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word OfficeArt color-alias recovery (2026-08-04): four registered Word 16.0 references prove that standalone `fillForeColor`, `fillBackColor`, `lineForeColor`, and `lineBackColor` properties do not replace the modern shape's default white fill or black outline. Word-compatible parsing now ignores the foreground aliases, treats `fillBackColor` only as a secondary pattern/gradient color, and ignores the unused line back color; StrictSpec retains bounded alias interpretation. All four corrected shapes match Word's white fill, black 0.75-point outline, and exact `(108,108)-(216,144)` frame. Numeric gradient fixtures continue to consume `fillBackColor` safely. Word/current pages were rendered at 4x and visually inspected; visible shape output matches exactly. Verification passes: 1,177 library, 40 conversion, 4 reference-policy, 1,012 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word foreground shape-text layer and insets (2026-08-04): the registered `shape-z-order-text-passive.rtf` PDF proves Word paints both ordinary body paragraphs before the z-order 23 floating shape, then paints the shape frame and its text as one foreground drawing layer. Layout now moves only explicitly ordered foreground drawing fragments after ordinary page content while retaining stable z-order and leaving zero-order inline images in source order; below-text drawings remain in the background. Modern OfficeArt shape text now defaults to Word's measured 144-twip (7.2-point) left/right insets while retaining the measured 80-twip (4-point) top/bottom inset. The corrected PDF matches Word's body/shape/text paint sequence, exact `(108,108)-(288,162)` frame, and shape-text x origin within 0.38 points; vertical glyph boxes retain the established bundled Arial-compatible residual. Word/current pages were rendered at 4x and visually inspected. Verification passes: 1,178 library, 40 conversion, 4 reference-policy, 1,012 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Legacy textbox malformed-recovery audit (2026-08-04): the registered Word 16.0 `legacy-textbox-passive.rtf` reference does not render the intended textbox. It emits only a zero-area 0.14-point stroke, leaks `hidden-legacy-textbox-coordinate-payload` into visible body text, drops `Legacy box text`, and consumes the `A` from `After legacy box.`. Matching that behavior would violate the passive security boundary and the fixture's visible-content contract. The converter intentionally retains its bounded legacy textbox with intended text, strips the nested OLE object and hidden coordinate payload, inserts the configured passive object-removal placeholder, and preserves the following paragraph. Word/current pages were rendered at 4x and visually inspected; the manifest now records this as an explicit security difference rather than an unaudited geometry gap.
- Word full-page background-shape normalization (2026-08-04): the registered `background-shape-passive.rtf` baseline proves that Word ignores the authored 150-by-60-point shape frame inside a `background` destination and paints its cyan fill across the complete 612-by-792-point page with a 0.1-point edge bleed, no outline, and body text above it. Background layout now derives a bounded page-anchored frame from each page's actual geometry, suppresses the ordinary shape outline, disables flow interaction, and keeps the normalized fill in the below-text layer. Hidden background text and unsupported payload metadata remain stripped. Word/current PDFs now both contain one cyan fill at `(0,0)-(612.1,792.1)` followed by body text; 4x renders were visually inspected and match apart from the shared bundled-font metric residual. Verification passes: 1,179 library, 40 conversion, 4 reference-policy, 1,012 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word page-border scope/reference recovery (2026-08-04): `page-border-scope-passive.rtf` proves parameterless `\pgbrdropt` means measure from the text region, the inverse of the prior parser flag. Word keeps the top/bottom/left borders at the 36-point page margins even with `\pgbrdrhead` and `\pgbrdrfoot` because the fixture has no actual header/footer content; layout now expands those scopes only when the selected header/footer set contains visible passive content. The default single page-border stroke is calibrated to Word's 0.72 points instead of the generic 0.5-point table-border default. Current output has the exact 36/270/360-point nominal bounds and the same three-sided scope; Word's filled-strip edges differ by at most 0.48 points from the converter's centered strokes. Word/current pages were rendered at 4x and visually inspected. Verification passes: 1,180 library, 40 conversion, 4 reference-policy, 1,012 security, 2 fuzz-regression, and 2 WASM dependency-policy tests; formatting and `git diff --check` pass.
- Word diagonal-table border recovery (2026-08-04): the registered `table-borders-passive.rtf` reference proves Word suppresses the authored row top/inner-horizontal/inner-vertical borders when the row also contains explicit diagonal cell borders, retaining only the first cell's explicit 1.44-point top/left edges and the second cell's blue solid and red fine-dashed diagonals. Word-compatible normalization now ignores that conflicting row-border layer while StrictSpec retains it. The explicit top border contributes Word's full stroke-width top inset to every cell baseline and half its left stroke to the first cell x origin; diagonal geometry begins below the top edge and expands the auto-height row. The red diagonal now uses Word's exact 0.14-point stroke and `[0.42 0.14]` dash cadence. Current line bounds are within 0.69 points horizontally and 0.24 points vertically of Word, while text boxes retain the established roughly 0.9-point bundled Arial metric residual. Word/current pages were rendered at 4x and visually inspected; raw row/cell controls remain outside the passive PDF.
- Diagonal-table verification (2026-08-04): complete gates pass: 1,181 library, 32 CLI, 40 conversion, 4 reference-policy, 1,012 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting and `git diff --check` pass.
- Focused WMF clipping audit (2026-08-05): `wmf-cliprect-passive.rtf` contains one bounded `IntersectClipRect` followed by a larger gray rectangle. Decoded Word operators confirm that Word retains the same gray fill and 1.87784-point black stock-pen stroke while clipping the paint through a nonuniform legacy metafile transform. The converter's visible clipped result differs by about 0.01 points at the left edge, 0.76 points at the right edge, and 0.70 to 1.73 points vertically; text/image flow remains identical apart from the established bundled Arial line-height residual. Word/current pages were rendered at 6x and visually inspected. The stale unaudited gap is now a measured bounded WMF playback-transform approximation; no speculative fixture-specific transform was added, and raw WMF bytes remain outside the PDF layer.
- Word inline-image paragraph-end flow (2026-08-05): the focused `wmf-hatch-brush-passive.rtf` comparison proves Word includes the non-painted paragraph mark's space advance when deciding whether the final word fits after an inline image. Layout now reserves that bounded advance only for the paragraph-final segment on an image line, wrapping final `hatch.` like Word while retaining the evidenced one-point body-edge tolerance for the non-final `After WMF dashed` segment. A line following an inline image now also reserves the missing quarter-line descender before normal flow, restoring Word's 13.8-point Arial baseline pitch; both hatch and dashed-pen follow-up lines are within 0.03 points vertically of Word. Focused regression coverage protects paragraph-final wrapping, image-line tolerance, and baseline pitch.
- Focused WMF hatch audit (2026-08-05): decoded/rasterized Word output confirms the red horizontal brush and black 1.87784-point stock-pen outline, but Word scales the legacy 32-by-32 bitmap tile into one roughly 5.21-point red band in this rectangle. The converter's hardened device-independent vector approximation emits six 0.35-point lines at 4-point spacing. A 12x crop makes the mismatch explicit; exact legacy bitmap-tile scaling and phase remain open rather than being replaced with a fixture-specific band. Raw pattern bytes remain outside the PDF.
- Inline-image flow verification (2026-08-05): complete gates pass after the paragraph-mark and baseline-pitch correction: 1,182 library, 40 conversion, 4 reference-policy, 1,012 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting and `git diff --check` pass.
- Completed Word baseline corpus reconciliation (2026-08-05): the automated run produced 171 Word 16.0 PDFs and retained five explicit export failures in `target/word365-auto/failures.csv`. Word rejects `carried-table-row-definitions-passive.rtf` as a corrupted table, rejects `font-template-metadata-passive.rtf` and `mailmerge-object-metadata-passive.rtf` for resource errors, cannot read `generated-reference-fields-passive.rtf`, and fails the cropped JPEG/DIB WMF export unexpectedly. These remain documented Word-side exceptions; no reference output is fabricated for them.
- Focused WMF SETPIXEL audit (2026-08-05): decoded Word output paints the authored red SETPIXEL as a 1.87784-point horizontal stroke from `(118.4671,64.6758)` to `(119.3248,64.6758)`. The converter preserves the color but emits a 0.54-by-0.36-point passive fill at `(114.5815,61.3188)-(115.1215,61.6788)`. Both pages were rendered at 8x and visually inspected. The displacement and scale agree with the broader nonuniform legacy WMF playback/map-transform gap also visible in clipping and hatch fixtures, so the manifest now records one general transform dependency instead of applying a fixture-specific offset or oversized pixel primitive. Raw WMF payloads remain outside the PDF layer.
- Word pixel-only EMF suppression (2026-08-05): the registered Word 16.0 `emf-setpixelv-passive.rtf` PDF contains no image, drawing, or spacing for its two SETPIXELV records; the surrounding text remains one uninterrupted line. Word-compatible parsing now identifies a successfully decoded EMF whose complete passive command set consists of SETPIXELV output and omits that inline picture with an explicit diagnostic. StrictSpec retains the prior bounded one-pixel rectangle fallback. Current/Word PDFs both extract the same visible sentence, contain zero drawings/images, and were rendered at 4x for visual inspection; only the established bundled Arial-compatible glyph metric residual remains. Raw EMF bytes and active PDF features remain stripped, and malformed/mixed-content EMFs retain bounded recovery behavior.
- Pixel-only EMF verification (2026-08-05): complete gates pass: 1,183 library, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word hatch-fill-only EMF suppression (2026-08-05): focused inspection of the registered Word `emf-hatch-brush-passive.rtf` PDF shows one uninterrupted text line and no image, drawing, Form XObject, or spacing for the decoded blue diagonal-cross Bezier fill. Word-compatible parsing now suppresses only successfully decoded EMFs whose complete visible command set consists of un-stroked hatch fills, with an explicit diagnostic; mixed-content EMFs remain eligible for bounded passive vector recovery. StrictSpec retains the prior clipped line-hatch fallback. Current/Word pages were rendered at 6x and visually inspected; output now has matching text flow and zero image/drawing objects while raw EMF payloads remain stripped.
- Hatch-fill-only EMF verification (2026-08-05): complete gates pass: 1,184 library, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word WMF SETPIXEL passive stroke (2026-08-05): decoded Word operators prove SETPIXEL is exported as a horizontal stroked segment rather than a filled rectangle. WMF parsing now emits a bounded line independent of selected pen state, using Word's measured 4.172978-logical-unit stock width and 1.5883-logical-unit one-device-pixel length. In the authored 200-by-100 to 108-by-36-point frame, current and Word PDFs now both contain a red 1.87784-point stroke exactly 0.8577 points long. Twelve-times renders were visually inspected. Word's command point remains about 3.89 points right and 3.36 points down from the standard logical mapping; exact placement remains documented pending multi-point Word evidence rather than introducing an underdetermined fixture-specific offset. Raw WMF bytes remain outside the PDF layer.
- WMF SETPIXEL stroke verification (2026-08-05): complete gates pass: 1,184 library, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word malformed-EMF reference-device suppression (2026-08-05): the completed Word 16.0 corpus proves all 13 registered `emf-*-passive.rtf` fixtures are omitted as zero-width inline content, independent of their decoded command families. Every fixture uses an 88-byte EMF header whose reference-device pixel and millimeter dimensions are all zero. Word-compatible parsing now suppresses successfully decoded EMFs with those unusable header metrics and emits one header-specific diagnostic; valid EMFs remain renderable, and StrictSpec retains the bounded passive vector fallback. Current and Word PDFs for all 13 fixtures now have identical one-page counts, identical normalized surrounding text, zero images, and zero drawings. A representative page was rendered at 4x and visually inspected. The executable reference gate and manifest now require this behavior and no longer preserve the EMF-internal `Hi` text that Word omits.
- Malformed-EMF suppression verification (2026-08-05): complete gates pass: 1,184 library, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word WMF POLYBEZIER no-op recovery (2026-08-05): the registered Word 16.0 `wmf-polybezier-passive.rtf` reference preserves the full 108-by-36-point inline picture flow but paints no cubic path, while the adjacent `wmf-polybezierto-passive.rtf` reference does paint its continuation path. Word-compatible WMF playback now treats the legacy `POLYBEZIER` extension record as non-painted while StrictSpec retains the bounded passive cubic preview. Current and Word outputs both contain one page, identical normalized text, zero images, and zero drawings; both pages were rendered at 4x and visually inspected and match apart from the established bundled-font residual.
- WMF POLYBEZIER no-op verification (2026-08-05): complete gates pass: 1,184 library, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word WMF PATCOPY no-op recovery (2026-08-05): registered Word references prove `BITBLT`, `STRETCHBLT`, `DIBSTRETCHBLT`, and `STRETCHDIB` PATCOPY records preserve the authored 108-by-36-point inline picture box but paint nothing, while adjacent `PATBLT` and `DIBBITBLT` references remain visibly rendered. Word-compatible playback now preserves that record-family distinction; StrictSpec retains bounded source-ignoring brush fills. The malformed STRETCHBLT fixture also ends with one dangling hex nibble, which Word ignores; Word-compatible parsing now discards only that incomplete WMF nibble and falls back to a blank bounded WMF box when the remaining malformed record stream cannot be decoded, instead of exposing an `Image skipped` placeholder. All four current/Word pairs have identical page counts, normalized text, image counts, and drawing counts. A representative pair was rendered at 4x and visually inspected.
- WMF PATCOPY no-op verification (2026-08-05): complete gates pass after moving the four existing passive-fill regressions to explicit StrictSpec coverage: 1,184 library, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word WMF SETDIBTODEV no-op recovery (2026-08-05): the registered Word 16.0 `wmf-setdib-to-dev-passive.rtf` reference preserves the authored inline picture flow but emits no raster image or vector drawing. Word-compatible WMF playback now treats SETDIBTODEV as non-painted, while StrictSpec retains bounded scan selection and passive RGB/PNG/JPEG normalization. Current and Word PDFs both contain one page, identical normalized text, zero images, and zero drawings. The current page was rendered at 4x and visually inspected; adjacent SRCCOPY DIB fixtures remain independently rendered.
- WMF SETDIBTODEV no-op verification (2026-08-05): complete gates pass: 1,184 library, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word WMF box-primitive playback recovery (2026-08-05): decoded Word Form XObjects establish that legacy WMF rectangles, clipping rectangles, roundrects, and ellipses without explicit viewport extents do not use the ordinary logical-coordinate map. Word-compatible playback now applies the measured bounded nonuniform transform to rectangle/clip/roundrect bounds and a separately measured ellipse transform; StrictSpec retains direct logical coordinates. The passive PDF writer now preserves independent horizontal and vertical ROUNDRECT radii instead of collapsing them to one circular radius. Current rectangle, ellipse, roundrect, and clipped-rectangle edges are within 0.10 points of Word, with exact 1.87784-point stock-pen widths and matching visible fills and paragraph flow. Current/Word vector, roundrect, and clip pages were rendered at 6x and visually inspected; the only visible residual is the established bundled Arial-compatible text metric difference. The hatch fixture shares the corrected outer rectangle but retains its separately documented legacy bitmap-tile scaling/phase gap. Raw WMF bytes remain outside the PDF layer.
- WMF box-primitive verification (2026-08-05): complete gates pass after moving legacy exact-coordinate security regressions to explicit StrictSpec coverage: 1,185 library, 32 CLI, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- WMF line/polygon transform evidence (2026-08-05): decoded Word `wmf-line-poly-paths-passive.pdf` Form operators show a second legacy playback map distinct from both box and ellipse records. For the authored 200-by-100 logical frame, x endpoints 20/180 recover to approximately 28.17/186.11 logical units, while y values 40/70/90 recover to approximately 50.00/81.97/103.74. Word also emits the four-point POLYLINE as one connected stroked path, whereas the converter currently emits three separately stroked segments. This is the next active parity feature; no underdetermined transform has been committed yet.
- Word WMF line/polygon playback recovery (2026-08-05): the measured five-x/four-y Word sample fit is now applied only to MOVETO/LINETO, POLYGON, POLYLINE, and POLYPOLYGON records in Word-compatible mode when no explicit viewport extent supersedes legacy recovery. StrictSpec retains direct logical mapping. Passive PDF POLYLINE output now emits one connected path and one stroke operation, preserving joins like Word instead of independently stroking every segment. The line/poly fixture now has Word's same three-drawing structure and edges within 0.13 points; the independent compound winding POLYPOLYGON fixture is within 0.12 points on every edge. Both current/Word pairs were rendered at 6x and visually inspected; fills, joins, paint order, stock-pen widths, and flow match apart from the established bundled-font residual. Raw WMF arrays remain bounded and outside the PDF layer.
- Word WMF custom-pen and POLYBEZIERTO recovery (2026-08-05): the completed Word references prove explicitly selected WMF pens retain the direct logical-coordinate map rather than the recovered stock-pen path map, and Word clamps authored one-unit pens to the same 1.87784-point minimum visible width as its four-unit pen. Word also ignores POLYBEZIERTO completely without advancing the current point, so the fixture paints only its original MoveTo-to-LineTo segment. Word-compatible playback now preserves those distinctions while StrictSpec retains bounded cubic playback and standards-style current-point updates. Current/Word custom-pen fixtures have matching command counts, stroke widths, dash cadence, and flow; residual edge placement is bounded to 1.73 points. The line/poly stock-pen fixture remains within 0.13 points. Raw object and point payloads remain outside the PDF layer.
- WMF path/custom-pen verification (2026-08-05): complete gates pass: 1,186 library, 32 CLI, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word WMF arc-family playback recovery (2026-08-05): decoded Word operators and 12x raster comparison prove legacy ARC uses the opposite 270-degree sweep from the converter's prior 90-degree path, while open ARC and closed CHORD/PIE use distinct bounded affine recovery maps. Word-compatible playback now reverses the sweep and applies the measured record-family transforms only when no explicit viewport extent supersedes legacy recovery; StrictSpec retains the previous specification-oriented direction and direct coordinates. Current and Word now share three drawings, gray fills, chord/pie closures, and 1.87784-point stock-pen strokes, with every visible bound within 0.08 points. The converter's bounded 48-segment contour is visually indistinguishable from Word's five-cubic contour at 12x. Raw WMF bytes remain outside the PDF layer.
- WMF arc-family verification (2026-08-05): complete gates pass after restoring the parser's transient workspace-corrupted range and revalidating prior Word recovery behavior: 1,186 library, 32 CLI, 40 conversion, 4 reference-policy, 1,013 security, 2 fuzz-regression, and 2 WASM dependency-policy tests. Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word WMF PATCOPY surviving-family recovery (2026-08-05): focused Word Form inspection proves PATBLT and DIBBITBLT PATCOPY retain their selected passive brush colors but use two distinct legacy playback maps. Word-compatible parsing now applies the measured bounded transform to PATBLT and only to DIBBITBLT PATCOPY when no explicit viewport extent supersedes recovery; StrictSpec retains direct destination coordinates, and the four separately evidenced PATCOPY no-op families remain suppressed. Current/Word PATBLT red and DIBBITBLT brown rectangles have matching paint and edges within 0.07 points. Source payload bytes remain stripped before the passive PDF boundary.
- WMF PATCOPY surviving-family verification (2026-08-05): complete affected gates pass after moving authored-coordinate raster-composition regressions to explicit StrictSpec coverage: 1,186 library, 40 conversion, 4 reference-policy, and 1,013 security tests. Formatting, manifest JSON parsing, and `git diff --check` pass; the unchanged CLI, fuzz-regression, and WASM dependency-policy gates had already passed for this implementation.
- Word WMF STRETCHDIB source-window orientation recovery (2026-08-06): focused inspection of the registered `wmf-srccopy-cropped-dib-passive.rtf` reference proves Word interprets positive `source_y` from the bottom of a bottom-up DIB for STRETCHDIB, while the adjacent DIBBITBLT record retains normalized top-down cropping. Word-compatible WMF playback now converts only positive-height STRETCHDIB source windows against validated BITMAPCOREHEADER/BITMAPINFOHEADER heights; StrictSpec and DIBBITBLT/DIBSTRETCHBLT retain their prior bounded behavior. Extracted current/Word image pixels now match exactly: the large crop is `(40,50,60)/(70,80,90)` and the small crop is `(130,140,150)/(160,170,180)`. Current and Word pages were rendered at 4x and visually inspected. Exact destination geometry remains measured in the reference policy as a bounded legacy playback-map residual rather than an underdetermined fixture-specific correction. Raw DIB and WMF payloads remain outside the PDF layer.
- WMF STRETCHDIB source-window verification (2026-08-06): complete gates pass: 1,187 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,013 security, and 2 WASM dependency-policy tests (2,280 total). Formatting, manifest JSON parsing, and `git diff --check` pass.
- Word malformed-JPEG omission and decode validation (2026-08-06): the registered `jpeg-trailing-payload-passive.rtf` baseline proves its frame-header-only pseudo-JPEG is not painted or given an inline image slot by Word. JPEG normalization now requires both a scan and successful bounded `jpeg-decoder` validation before bytes can cross into a PDF DCT image, including JPEG-in-DIB metafile paths. Word-compatible mode retains a zero-width anchor for malformed JPEGs so surrounding text stays contiguous; StrictSpec retains the explicit malformed-image placeholder. Current and Word PDFs both contain no images/drawings, share the exact visible sentence, and were rendered at 8x; text starts differ by 0.03 points horizontally and retain the shared roughly 0.95-point bundled Arial vertical residual. Malformed JPEG bytes, trailing active-looking payloads, controls, and PDF active features remain stripped. Parser/security regressions cover missing scans, undecodable scans, StrictSpec behavior, binary payload opacity, and DCT/Image-marker absence.
- Decoder-valid JPEG fixture repair and verification (2026-08-06): parser and security test JPEG helpers now use real RGB, grayscale, and CMYK scan data; large ignored APP segments preserve explicit passive PDF-decode fallback coverage without corrupt inputs. The cropped JPEG-in-DIB WMF fixture now carries a decoder-valid 3-by-2 JPEG and renders two passive clipped raster draws successfully at 8x. Word 16.0 still rejects that fixture during a fresh single-fixture export with its generic unexpected-error result, so the missing baseline remains an explicit Word-side exception. Complete gates pass after correcting two stale Word-mode/fallback expectations: 1,189 library, 32 CLI, 40 conversion, 2 fuzz-regression, 4 reference-policy, 1,014 security, and 2 WASM dependency-policy tests (2,283 total). Formatting, manifest JSON parsing, and `git diff --check` pass.
- Focused Word shape-flip audit (2026-08-06): `shape-flip-passive.rtf` already matches Word's horizontal-plus-vertical mirrored line exactly. Both PDFs contain one red 2-point stroke from `(216,144)` to `(108,108)` points, identical drawing/page counts and flow, and no image or active objects. Eight-times renders were visually inspected; remaining text displacement is the shared 0.03-point horizontal and roughly 0.95-point bundled Arial-compatible vertical metric residual. The stale unaudited manifest gap is closed without a speculative renderer change.
- Focused Word column-section-break audit (2026-08-06): `column-section-break-passive.rtf` already matches Word's one-page `\sbkcol\sect` recovery by moving the second paragraph to the top of column two. Current left/right origins are `(36.00,35.49)` and `(171.00,35.49)` points versus Word's `(36.02,36.44)` and `(171.07,36.44)` points. Eight-times renders confirm identical same-baseline column flow; the residual is limited to shared bundled Arial-compatible width and roughly 0.95-point vertical metrics. The stale unaudited manifest gap is closed without a renderer change.
